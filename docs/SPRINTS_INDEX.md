# SPRINTS_INDEX.md — HANDYMAX · Multimax Despacho

Índice oficial del desarrollo por Sprints incrementales (metodología vigente desde el Sprint 3.1). Cada Sprint migra una sección específica de `Multimax_Despacho_v1.3.html`. Este archivo se actualiza al finalizar cada Sprint — ver `docs/sprints/sprint-X.Y.md` para el detalle de cada uno.

**Desde el Sprint 3.2**, cada Sprint se identifica por el bloque/selector real del HTML (no por un nombre genérico de sección como "Sidebar"/"Navigation"/"Dashboard"). La columna "Elemento" de los Sprints 3.15 en adelante todavía usa los nombres genéricos originales porque no se ha hecho el análisis previo obligatorio de esos bloques — se actualizarán al selector real del HTML cuando le corresponda su turno, sin adelantar ese análisis ahora. El Sprint 3.4 confirmó, por inspección directa del HTML (no por el nombre genérico que tenía esta tabla, "Main Layout"), que el bloque real pendiente era `mx-suc-sel`; el Sprint 3.5 verificó que "Publish Modal" sí correspondía al bloque real (`PublishModal`); el Sprint 3.6 determinó que "Job Cards" **no** correspondía al bloque real reconstruible ahora mismo; el Sprint 3.7 confirmó que "Radar" **sí** es un nombre real (`function Radar`), pero descartó la arquitectura multi-componente que sugería el brief; el Sprint 3.8 confirmó que "Countdown"/`CountRing` **sí** es un nombre real, y reportó que existe un segundo componente de countdown distinto (`LiveCountdown`) sin Sprint asignado en ese momento; el Sprint 3.9 asignó y reconstruyó ese componente (`LiveCountdown`), descartando el nombre genérico "Timeline" que traía esta tabla; el Sprint 3.10 determinó que "Installer Dashboard" **no** es el nombre de ninguna función real — el equivalente real es `Installer(props)`, del cual se reconstruyó únicamente el subconjunto alcanzable sin invadir los Sprints ya reservados 3.11 (`InstallerProfile`)/3.12 (`InstallerJobs`); el Sprint 3.11 confirmó que "Installer Profile" **sí** corresponde exactamente a una función real (`function InstallerProfile({ meInfo })`) — a diferencia de la mayoría de los Sprints anteriores, sin corrección de nombre necesaria; el Sprint 3.12 confirmó que "InstallerJobs" **sí** corresponde exactamente a una función real (`function InstallerJobs()`); el Sprint 3.13 determinó que "Admin Dashboard" **no** es el nombre de ninguna función real — el equivalente real es `AdminPanel()`; el Sprint 3.14 confirmó que "Calendar" **sí** corresponde exactamente a una función real (`function MasterCalendar()`) — ver notas abajo.

| Sprint | Estado | Elemento | Rama Git |
|---------|--------|----------|-----------|
| 3.1 | ✅ Completado | Header (`<header class="mx-top">`) | `feature/sprint-3-1-header` |
| 3.2 | 🟡 En progreso — pendiente validación local del usuario | `mx-instside` (panel lateral del Instalador) | `feature/sprint-3-2-mx-instside` |
| 3.3 | ✅ Completado | `mx-subtabs` (Top Navigation) | `feature/sprint-3-3-mx-subtabs` |
| 3.4 | ✅ Completado | `mx-suc-sel` (selector de sucursal activa) | `feature/sprint-3-4-mx-suc-sel` |
| 3.5 | ✅ Completado | `PublishModal` (formulario de publicar trabajo) | `feature/sprint-3-5-publish-modal` |
| 3.6 | ✅ Completado | `CoordinatorEmptyState` (estado vacío de `Coordinator`, `mx-qempty`) — no "Job Cards" (`mx-jobcard`), ver notas abajo | `feature/sprint-3-6-coordinator-empty-state` |
| 3.7 | ✅ Completado | `Radar` (panel SVG de instaladores, `function Radar`) — sin sub-componentes, no incluye `CountRing`, ver notas abajo | `feature/sprint-3-7-radar` |
| 3.8 | ✅ Completado | `CountRing` (anillo SVG de countdown, `function CountRing`) — no incluye `LiveCountdown`, ver notas abajo | `feature/sprint-3-8-countdown` |
| 3.9 | ✅ Completado | `LiveCountdown` (countdown de texto para trabajos en cola, `function LiveCountdown`) — no incluye `CountRing`, ver notas abajo | `feature/sprint-3-9-live-countdown` |
| 3.10 | ✅ Completado | `InstallerDashboard` (subconjunto reconstruible de `function Installer(props)`: barra del teléfono, navegación, estado vacío de "Solicitudes") — no incluye `InstallerJobs`/`InstallerProfile`, ver notas abajo | `feature/sprint-3-10-installer-dashboard` |
| 3.11 | ✅ Completado | `InstallerProfile` (pantalla "Perfil" del teléfono del Instalador, `function InstallerProfile({ meInfo })`) — nombre del brief confirmado exacto, ver notas abajo | `feature/sprint-3-11-installer-profile` |
| 3.12 | ✅ Completado | `InstallerJobs` (pantalla "Mis trabajos" del teléfono del Instalador, `function InstallerJobs()`) — nombre del brief confirmado exacto, ver notas abajo | `feature/sprint-3-12-installer-jobs` |
| 3.13 | ✅ Completado | `AdminPanel`/`AdminInstaladores` (raíz del panel de Administrador, `function AdminPanel()` + `function AdminInstaladores()`) — no "Admin Dashboard" (nombre genérico, sin función real equivalente), ver notas abajo | `feature/sprint-3-13-admin-dashboard` |
| 3.14 | ✅ Completado | `MasterCalendar` (calendario maestro del panel de Administrador, `function MasterCalendar()`) — nombre "Calendar" del brief confirmado exacto, integrado dentro de `AdminPanel`, ver notas abajo | `feature/sprint-3-14-calendar` |
| 3.15 | ✅ Completado | `ConfirmCancelDialog` (`function ConfirmCancel({ onYes, onNo })`) — no "Shared Dialogs" (nombre genérico), ver notas abajo | `feature/sprint-3-15-dialogs` |
| 3.16 | ✅ Completado | `AssignedPanel`/`NoResponsePanel` (bloques de `Coordinator(props)` reconstruibles sin `jobs.length > 0`) — no "Shared Components" (nombre genérico) | `feature/sprint-3-16-shared-components` |

> **Nota (Sprint 5.1, 2026-07-22)**: esta tabla quedó desactualizada tras el Sprint 3.14 — 3.15/3.16 ya estaban completados (confirmado por el JSDoc histórico de `RootLayout.tsx` y por `TODO.md`), pero seguían marcados "⏳ Pendiente" acá; se corrige en esta misma edición. A partir de Fase 4 (backend: Sprints 4.0.1, 4.0.2, 4.1.1 y sub-iteraciones, 4.1.1B, 4.1.1C, 4.2.1) el seguimiento Sprint-a-Sprint se llevó en `PROJECT_STATUS.md`/`PHASE_4.md` (formato distinto, ya no "un bloque HTML por Sprint" sino infraestructura/Auth) -- este archivo no se extendió para esos Sprints. Fase 5 ("Flujo Operativo") retoma este archivo desde Sprint 5.1 -- ver fila y detalle abajo -- sin backfillear retroactivamente las filas de Fase 4 (fuera de alcance de este Sprint; `PROJECT_STATUS.md`/`PHASE_4.md` siguen siendo la fuente autorizada para esos Sprints).

| 5.1 | ✅ Completado | `DespachoPage`/`TrabajosPage`/`TrabajoDetailPage` (rutas reales `/despacho`/`/trabajos`/`/trabajos/:id`, `ARCHITECTURE.md §8`) — primer Sprint de Fase 5 ("Flujo Operativo") | *(sin rama Git — flujo Git manual del usuario)* |
| 5.1.1 | 🟡 En revisión | `AdminVistaSwitch` (selector temporal "Modo de Visualización del Administrador" — Administración/Coordinador/Instalador, exclusivo de `admin`, sin tocar Auth/Supabase/RLS) — Sprint de navegación puro, sin funcionalidades nuevas. **Ajuste final (misma fecha)**: nueva abstracción permanente `OperationalContextProvider`/`useOperationalContext()` (`src/providers/`, `src/hooks/`), resuelve empresa/tienda reales (tablas `empresas`/`tiendas`) para el admin en Modo Coordinador sin usar `profile.tiendaId`; Coordinador real sin cambios. Ver `docs/architecture/frontend/SPRINT_5_1_1_ADMIN_SUPERUSER_REPORT.md` (secc. 11) | *(sin rama Git — flujo Git manual del usuario)* |
| 5.1.2 | 🟡 En revisión | `CoordinatorLayout` (NUEVO, `src/layouts/`) — único punto de entrada de toda la operación del Coordinador; reorganiza `Header`/`Footer`/`SucursalSelect`/`CoordinatorSubtabs`/`PublishModal`/`ConfirmCancelDialog` (antes repartidos entre `RootLayout.tsx` y duplicados en `DespachoPage`/`TrabajosPage`) sin duplicar ningún componente, sin rutas nuevas, sin cambio de comportamiento. `CoordinatorSidebar` explícitamente NO creado (HTML oficial no lo tiene, decisión reafirmada por el usuario). Ver `docs/architecture/frontend/SPRINT_5_1_2_COORDINATOR_LAYOUT_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.1.3 | 🟡 En revisión | `JobSummaryCard`/`LiveDispatchCard`/`ResponsesPanel` (NUEVOS, `src/components/shared/`) — reconstruyen el Workspace Operativo real de `Coordinator()` (rama `jobs.length>0`, nunca portada antes) dentro de `DespachoPage`, con un job de demostración fijo (fase `live`). Reemplaza el estado vacío `CoordinatorEmptyState`+`Radar`+`LiveCountdown` sueltos (decisión explícita del usuario). Reutiliza `TwoColumnLayout`/`Radar`/`LiveCountdown`/`CoordinatorKpiRow` sin cambios. Ver `docs/architecture/frontend/SPRINT_5_1_3_COORDINATOR_WORKSPACE_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.1.4 | 🟡 En revisión | `JobIndicadoresCard` (NUEVO, `src/components/shared/`) + estado único de control `activeJob` en `DespachoPage` — cierra la serie 5.1.x: corrige que el Workspace se mostrara siempre (ahora `CoordinatorEmptyState`/Workspace son mutuamente excluyentes, Reglas 19/21) y envuelve `CoordinatorKpiRow` (SIN CAMBIOS, decisión explícita del usuario) con el marco visual real del bloque "Indicadores" (`mx-goal`, CSS nunca portado). `JobSummaryCard`/`ResponsesPanel` verificados completos sin cambios necesarios. Ver `docs/architecture/frontend/SPRINT_5_1_4_COORDINATOR_WORKSPACE_COMPLETION_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.1.5 | 🟡 En revisión | Corrección visual (Reglas 23/24) — `activeJob` ya NO inicia en `JOB_DEMO` (nuevo flag `DEMO_MODE`, `false` por defecto, `JOB_DEMO` permanece como dato temporal sin auto-renderizar); se agrega el Pill real faltante "Urgente"/"Normal" en `JobSummaryCard` (`mx-jobcard-h`); se retira el mensaje de error (`kpisError`) de dentro de `JobIndicadoresCard` (el HTML oficial no muestra errores en el bloque "Indicadores") y se reubica fuera de ese bloque en `DespachoPage`. `CoordinatorKpiRow` sin cambios. Ver `docs/architecture/frontend/SPRINT_5_1_5_COORDINATOR_LAYOUT_FIX_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.1 | 🟡 En revisión | Publish Workflow (Estado Local MVP) — flujo real `CoordinatorEmptyState`→`PublishModal`→confirmar→Job temporal en memoria→`activeJob`→`CoordinatorWorkspace`, sin Supabase/persistencia. `JOB_DEMO`/`DEMO_MODE` retirados por completo de `DespachoPage.tsx` (Regla 17); nuevo estado `activeJob`/`setActiveJob` y `onPublish` real agregados a `CoordinatorLayout.tsx` (excepción mínima y autorizada a la Regla 12, único propietario de `PublishModal` sin cambios). Ver `docs/architecture/frontend/SPRINT_5_2_1_PUBLISH_WORKFLOW_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.1 Fix | 🟡 En revisión | Publish Workflow Stabilization — `activeJob`/`setActiveJob` se mueven de `CoordinatorLayout.tsx` a `OperationalContextProvider.tsx` (única forma de sobrevivir la navegación Coordinador↔Instalador↔Administración sin subir a `RootLayout.tsx` ni crear un Context nuevo, decisión autorizada por el usuario tras 2 rondas de auditoría); botón "Cancelar" conectado (`activeJob→null→CoordinatorEmptyState`); validaciones de `PublishModal` (7 campos, sin librerías externas); corrección de la causa real del bloqueo indefinido de `CoordinatorKpiRow` (`.catch()` faltante en `DespachoPage.tsx`, no en la capa Supabase). Ver `docs/architecture/frontend/SPRINT_5_2_1_PUBLISH_WORKFLOW_FIX_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.1 Fix (KPI Loading) | 🟡 En revisión | Coordinator KPI Loading Resolution — causa raíz real del bloqueo indefinido de `CoordinatorKpiRow`: no existía ningún estado `loading` explícito, `JobIndicadoresCard` inferí­a "cargando" de `kpis === null`, incorrecto para cualquier desenlace terminado sin datos (error real de Postgrest/RLS, no solo la promesa rechazada ya cubierta en la ronda anterior). Se agrega `kpisLoading` (nuevo `useState`, `DespachoPage.tsx`) con recorrido `true→...→false` garantizado en todos los caminos de salida (incluido `.finally()`); `JobIndicadoresCard` ahora gobierna su `<Loading/>` por ese estado, no por `kpis`. Ver `docs/architecture/frontend/SPRINT_5_2_1_KPI_LOADING_FIX_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.1 Fix (corrección — KPI siempre visible) | 🟡 En revisión | Corrección puntual posterior, instrucción directa del usuario (sin brief nuevo): `CoordinatorKpiRow` no debe ocultarse nunca. Se retira el modelo `kpisLoading`/`<Loading/>` de la fila anterior; `kpis` (`DespachoPage.tsx`) pasa a ser siempre un objeto `CoordinatorKpis` válido (nueva constante local `ZERO_KPIS`, no un mock, mismo valor que `calcularKpis([])` ya producía), nunca `null`. `JobIndicadoresCard` ya no tiene ninguna rama condicional — siempre `<CoordinatorKpiRow kpis={kpis}/>`. `coordinator-kpi-row.tsx` sin cambios. Ver Anexo (sección 10) de `docs/architecture/frontend/SPRINT_5_2_1_KPI_LOADING_FIX_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.2.1 | 🟡 En revisión | Persistencia del trabajo publicado (Supabase) — primer Sprint de esta Fase que escribe datos reales: `onPublish` (`CoordinatorLayout.tsx`) pasa de un Job 100% en memoria a un `INSERT` real en `trabajos` (`trabajosRepository.create()`, sin cambios), mapeando la fila devuelta (+ `form.sucursal`) a `ActiveJob` antes de `setActiveJob()`. 2 consultas previas al usuario antes de escribir código: (1) confirmación de tabla/RLS — se encontró que un `admin` en modo superusuario probablemente no satisface la policy real "coordinadores publican en su tienda"; el usuario decidió no tomar ninguna decisión arquitectónica todavía y preparar el flujo para la primera prueba real con el Coordinador ya sembrado; (2) mecanismo de error — se autorizó reutilizar el Toast ya existente (`ui/toast.tsx`, mismo patrón de `LoginPage.tsx`), sin tocar `FieldError`/`PublishModal`. Ver `docs/architecture/frontend/SPRINT_5_2_2_1_SUPABASE_PUBLISH_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.2.1 Fix (RLS recursion) | 🟡 En revisión (SQL listo, pendiente de ejecución/validación del usuario) | Auditoría y corrección de RLS Policies de `trabajos` — esa primera prueba real (fila anterior) sí se ejecutó: falla con `42P17 infinite recursion detected in policy for relation "trabajos"`. Causa identificada y CONFIRMADA por el usuario contra `pg_policies` real: ciclo de 2 vías entre la policy SELECT de `trabajos` "instaladores ven trabajos donde fueron notificados" (subconsulta a `trabajo_instaladores`) y las 3 policies de `trabajo_instaladores` de gestión de notificaciones (subconsulta a `trabajos`+`coordinadores`) — ninguna usa `SECURITY DEFINER` (0 en Producción), Postgres detecta el ciclo al planificar el `INSERT...RETURNING`. Corrección: función `SECURITY DEFINER` `instalador_fue_notificado()` que reemplaza la subconsulta directa de esa única policy — rompe el ciclo sin tocar policies de Coordinador/Admin. Cero cambios de frontend. Ver `docs/architecture/backend/SPRINT_5_2_2_1_RLS_POLICY_AUDIT_REPORT.md` (reporte) y `SPRINT_5_2_2_1_RLS_POLICY_FIX.sql` (SQL ejecutable) | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.2.2 (GRANT fix) | 🟡 En revisión (SQL listo, pendiente de ejecución/validación del usuario) | Auditoría y corrección de privilegios SQL (GRANT) para publicación de trabajos — con el `42P17` ya corregido (fila anterior), el INSERT de Publish ahora falla con `42501 permission denied for table trabajos`: rechazo de la capa de privilegios SQL estándar (evaluada siempre antes que RLS). Auditoría descartó por evidencia directa "falta USAGE sobre secuencia" (`trabajos.id` es `uuid DEFAULT gen_random_uuid()`, sin `SERIAL`/`IDENTITY`/secuencia) y confirmó que ninguna migración del repositorio jamás incluyó un `GRANT`/`REVOKE` — mismo patrón ya confirmado en el Sprint 4.2.1 (privilegios de tabla aplicados manualmente en el Dashboard, nunca formalizados). Corrección: `GRANT INSERT, SELECT ON TABLE public.trabajos TO authenticated;` (el `SELECT` es necesario porque `.insert().select().single()` es un único `INSERT...RETURNING`). Cero cambios de frontend, RLS, o funciones SQL. Ver `docs/architecture/backend/SPRINT_5_2_2_2_SQL_GRANTS_FIX.sql` (SQL ejecutable; diagnóstico completo en `PROJECT_STATUS.md`/`CHANGELOG.md`) | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3 | 🟡 En revisión | Sucursal activa en toda la vista Coordinador — auditoría primero: "Mis trabajos"/KPIs ya reaccionaban al cambio de sucursal (queries vía `useEffect([tiendaId, ...])`); `activeJob` ("Despacho en vivo") es un `useState` independiente en `OperationalContextProvider`, sin relación con `tiendaId`, por eso seguía mostrando el mismo trabajo. Hallazgo adicional: el badge de sucursal del Header nunca reaccionó al selector (bug heredado del Sprint 3.4, documentado, no corregido). Implementación: nuevo `useEffect(() => setActiveJob(null), [tiendaId])` en `OperationalContextProvider.tsx` (único archivo de código) -- limpia el trabajo activo al cambiar de tienda, ocultando en cascada Radar/Respuestas/KPIs de un trabajo obsoleto sin tocar `DespachoPage.tsx`. Sin Context nuevo, sin cambios de UI/RLS/repositorios. Ver `docs/architecture/frontend/SPRINT_5_2_3_SUCURSAL_CONTEXT_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3.1 | 🟡 En revisión | Corrección definitiva del selector "Sucursal activa" — reauditoría tras reporte del usuario de que 5.2.3 no bastaba. Causa raíz real (nuevo hallazgo): `sucursalCoord` (`RootLayout.tsx`) nacía como literal fijo `'Multiplaza'`, nunca sincronizado con la tienda real de un Coordinador autenticado -- el dropdown podía mostrar cualquier sucursal mientras las queries reales seguían usando, correctamente, `profile.tiendaId` fijo. Especificación confirmada por el usuario (tras `AskUserQuestion`): Coordinador real → tienda auto-cargada al iniciar sesión, selector visible con todas las demás opciones `disabled`, no editable; Admin en Modo Coordinador → selector 100% libre. Implementación (3 archivos, sin tocar datos/RLS/repositorios): `RootLayout.tsx` sincroniza `sucursalCoord` con `profile.tiendaNombre`; `CoordinatorLayout.tsx` reutiliza `esSuperusuario`/`tiendaNombre` (ya existían en `OperationalContextValue`) para calcular el bloqueo; `sucursal-select.tsx` agrega prop `enabledValue`. Hallazgo reportado, no resuelto (fuera de alcance): `empresas`/`tiendas` tienen RLS habilitado con cero policies para `authenticated` (nunca corregido a diferencia de `admins`/`coordinadores`, Sprint 4.2.1) -- explica por qué Modo Coordinador puede seguir sin ser observable, aunque el código ya es correcto. Ver `docs/architecture/frontend/SPRINT_5_2_3_1_SUCURSAL_OPERATIONAL_CONTEXT_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3.2 | 🟡 En revisión | Consistencia completa del selector de sucursal para Coordinador — auditoría confirmó una segunda fuente de verdad real: `PublishModal` usaba la prop `sucursal` solo como valor inicial de su propio `useState` (`f.sucursal`), nunca resincronizado (el modal nunca se desmonta), mientras su `<Select>` de "Sucursal que publica" ofrecía las 9 sucursales sin restricción. Corrección (2 archivos, sin Context nuevo, sin recalcular lógica): `CoordinatorLayout.tsx` pasa el mismo `sucursalLockValue` ya calculado en el Sprint 5.2.3.1 como nueva prop `enabledValue` a `PublishModal`; `publish-modal.tsx` deshabilita las opciones salvo la bloqueada (mismo criterio que `SucursalSelect`) y sincroniza `f.sucursal` mientras el modal está bloqueado -- inerte para Admin en Modo Coordinador. Única fuente de verdad: `esSuperusuario`/`tiendaNombre` → `sucursalLockValue` → reutilizado por ambos selectores. Sin cambios a Supabase/RLS/repositorios/backend. Ver `docs/architecture/frontend/SPRINT_5_2_3_2_PUBLICAR_SUCURSAL_CONTEXT_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3.3 | 🟡 Auditoría cerrada, corrección pendiente | Auditoría completa del Contexto Operativo del Coordinador — Sprint exclusivamente de diagnóstico, sin ningún cambio de código, a pedido explícito del usuario tras reportar que el selector principal ahora "intenta bloquear las sucursales" y el selector del modal deja TODAS las sucursales deshabilitadas (formulario nunca enviable). Causa raíz identificada con evidencia línea por línea: `tiendaNombre` (`profile.service.ts` → `resolveTiendaNombre()` → `tiendasRepository.getById()`) resuelve a `null` porque `tiendas` (y `empresas`) tienen RLS habilitado sin ninguna policy de `SELECT` para `authenticated` (documentado sin corrección desde el Sprint 4.0.1/4.1.1, confirmado no resuelto en `SPRINT_4_2_1_AUTH_REPORT.md` §12.2-12.4 — solo `admins` se corrigió manualmente). Ese `null` se propaga sin transformación por `OperationalContextProvider` (passthrough) → `CoordinatorLayout.sucursalLockValue = ''` → `SucursalSelect`/`PublishModal` deshabilitan las 9 opciones (ninguna sucursal real se llama `''`) → `PublishForm.sucursal` queda forzado a `''` → validación rechaza el envío siempre. El mecanismo de bloqueo en sí (Sprints 5.2.3.1/5.2.3.2) es correcto; el dato de entrada nunca lo fue. Propuesta para el Sprint siguiente (NO implementada): agregar policy de `SELECT` para `authenticated` en `tiendas`/`empresas`, y verificar si `coordinadores` también la necesita (ambigüedad documental, sin confirmar). Ver `docs/architecture/frontend/SPRINT_5_2_3_3_COORDINATOR_OPERATIONAL_CONTEXT_AUDIT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3.4 | 🟢 SQL listo, pendiente de ejecución/validación del usuario | Corrección de acceso RLS para `public.tiendas` — continuación directa del Sprint 5.2.3.3. Primera sub-ronda: auditoría documental, bloqueada por falta de acceso en vivo a Supabase, entregó 3 consultas exactas al usuario (`pg_class`/`pg_policies`/`information_schema.role_table_grants`) — ver `docs/architecture/backend/SPRINT_5_2_3_4_RLS_MASTER_TABLES_REPORT.md`. Segunda sub-ronda (esta): el usuario ejecutó esas consultas contra Producción y confirmó, con evidencia real, que `empresas`/`coordinadores` ya tienen policy de `SELECT` funcional (nada que tocar) y GRANTs completos en las 3 tablas (nada que tocar) — el único hallazgo real es que `tiendas` tiene RLS habilitado, GRANT `SELECT` presente, pero **cero policies**, exactamente la combinación que produce "0 filas sin error" → `tiendaNombre = null`. Corrección: única policy nueva `"usuarios autenticados pueden leer tiendas"` (`FOR SELECT TO authenticated USING (true)`), replicando el mismo patrón ya confirmado y funcional de la policy real de `empresas` — no un modelo nuevo. Alcance estrictamente acotado a `tiendas`: cero cambios a `empresas`/`coordinadores`/`trabajos`/GRANTs/Auth/`src/`. SQL ejecutable en `docs/architecture/backend/SPRINT_5_2_3_4_RLS_TIENDAS_FIX.sql` (verificación previa + corrección + validación + rollback); diagnóstico completo en `docs/architecture/backend/SPRINT_5_2_3_4_RLS_TIENDAS_REPORT.md`. Pendiente de que el usuario ejecute el SQL en Producción y confirme la validación funcional. | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3.3.1 | 🟢 Auditoría cerrada — flujo ya conectado, sin bug de código encontrado | Persistencia real del flujo Publish — el usuario adjuntó un ZIP (`handymaxdespachofixpublishwoks.zip`) reportando que Publish no inserta en `public.trabajos` aunque la UI lo muestre. Auditoría reveló que ese ZIP corresponde al Sprint 5.2.1 Fix (23 de julio), ANTERIOR al Sprint 5.2.2.1 que ya agregó la persistencia real — consultado el usuario (`AskUserQuestion`), confirmó ignorar el ZIP y auditar el entorno de trabajo actual. Con evidencia línea por línea (`CoordinatorLayout.tsx` → `trabajosRepository.create()` → `INSERT...RETURNING` real → `activeJob`/cierre del modal SOLO en la rama de éxito, nunca antes de que Supabase confirme la fila) se demuestra que el flujo YA está correctamente conectado desde el Sprint 5.2.2.1 — no se encontró ningún camino de código que explique el síntoma reportado; hipótesis más consistente con la evidencia: la prueba se hizo contra una build vieja (el propio ZIP adjunto es prueba de que existe una copia en ese estado). Único cambio de código (hallazgo secundario real, dentro del alcance "recargar KPIs" del brief): `DespachoPage.tsx` no recargaba `CoordinatorKpiRow` tras un Publish/Cancelar exitoso en la misma sesión (efecto sin `activeJob` en sus dependencias) — corregido agregando `activeJob?.id` a esas dependencias. `CoordinatorLayout.tsx`/`trabajos.repository.ts`/`publish-modal.tsx`/RLS/Auth: sin cambios, ya correctos. Ver `docs/architecture/frontend/SPRINT_5_2_3_3_1_PUBLISH_PERSISTENCE_AUDIT_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3.x | 🟢 Corregido en frontend — pendiente SQL 5.2.3.4 para validar con datos reales | Corrección definitiva del contexto operativo del Coordinador — objetivo explícito del brief: una sola fuente de verdad (`OperationalContext`) para badge superior, selector superior, `PublishModal`, "Mis Trabajos" y "Despacho en Vivo". Auditoría de los 10 términos pedidos (`tiendaId`/`tiendaNombre`/`sucursal`/`sucursalCoord`/`selectedSucursal`/`enabledValue`/`lockValue`/`form.sucursal`/`activeSucursal`/`activeStore`) confirmó 2 duplicidades reales: (1) badge superior — `CoordinatorLayout.tsx` nunca pasaba `sucursalActiva` a `<Header>`, bug heredado y documentado sin corregir desde el Sprint 3.4, `HeaderStatus` mostraba siempre su default hardcodeado `'Multiplaza'`; (2) selector superior — su `value` venía de `sucursalCoord` (`RootLayout.tsx`), un `useState` con literal fijo `'Multiplaza'` y un `useEffect` de sincronización unidireccional (Sprint 5.2.3.1) que nunca corría mientras `tiendaNombre` fuera `null`, dejando un snapshot obsoleto. Corrección (2 archivos, sin Context nuevo, sin estado nuevo, sin tocar RLS/Repository/Backend): `RootLayout.tsx` retira el `useEffect` de sincronización (snapshot antiguo); `CoordinatorLayout.tsx` agrega `sucursalDisplayValue = esSuperusuario ? sucursalCoord : (tiendaNombre ?? '')`, misma fórmula que `sucursalLockValue` ya usaba, y lo pasa a `Header`/`SucursalSelect`/`PublishModal`. `TrabajosPage.tsx`/`DespachoPage.tsx` verificados ya correctos (leen `OperationalContext` directo), sin cambios. `selectedSucursal`/`activeSucursal`/`activeStore` confirmados ausentes del código (grep, cero resultados). Rama pedida por el brief (`feature/sprint-5.2.3-sucursal-filter`) no existe — mismo patrón sin ramas Git desde la Fase 4. Sandbox sin `node_modules`/red: no pudo ejecutar `lint`/`typecheck`/`build`/`dev` reales, solo `tsc --noEmit` best-effort (sin errores atribuibles a los cambios). Ver `docs/architecture/frontend/SPRINT_5_2_3_X_OPERATIONAL_CONTEXT_SYNC_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3.4 (implementación) | 🟡 SQL confirmado listo — ejecución/validación en Producción pendiente del usuario | Corrección de política RLS para `public.tiendas`, ronda de implementación tras nueva evidencia del usuario: `SELECT` directo confirma que la fila existe (`Multimax Paitilla`), pero `GET /rest/v1/tiendas?id=eq....` vía PostgREST (como `authenticated`) devuelve `HTTP 200` con `[]` — mismo patrón "RLS habilitado, cero policies" ya diagnosticado. Auditoría previa exigida por el brief (`profile.service.ts` → `resolveTiendaNombre()`, `tiendas.repository.ts` → `getById()`, consulta REST generada): sin diferencias respecto a la ronda anterior, confirmado que sigue siendo `SELECT * FROM tiendas WHERE id = $1`. El SQL de corrección ya generado en la ronda anterior (`SPRINT_5_2_3_4_RLS_TIENDAS_FIX.sql`) se verificó que cumple, sin cambios, todas las restricciones de esta ronda (RLS habilitado, solo `SELECT`, sin tocar `empresas`/`coordinadores`) — no se generó un SQL duplicado. Alcance: cero archivos de `src/` tocados (verificado). No fue posible ejecutar el SQL contra Producción ni reconsultar el `GET` desde este entorno (sin acceso de red a Supabase) — se documentaron los 4 pasos exactos que el usuario debe ejecutar y reportar para cerrar el criterio de éxito del brief. Ver `docs/architecture/backend/SPRINT_5_2_3_4_RLS_TIENDAS_IMPLEMENTATION_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 5.2.3.5 | 🟢 Corregido — RLS ya validada por el usuario en Producción (badge y `GET /rest/v1/tiendas` confirmados correctos) | Sincronización del selector de sucursal (`SucursalSelect`) — con la RLS de `tiendas` ya validada por el usuario (badge superior mostrando "Multimax Paitilla" correctamente), el único síntoma restante era el selector. Auditoría (exclusiva a `SucursalSelect`/`CoordinatorLayout`/`RootLayout`, sin tocar RLS/Repositorios/Servicios/`OperationalContext`/Auth) encontró la causa raíz real: `SUCURSALES` (`constants/index.ts`) sigue siendo la lista LITERAL de 9 nombres del HTML original (Sprint 3.4), nunca reemplazada por datos reales — el nombre real de una tienda ya migrada (`"Multimax Paitilla"`) no está entre esos 9, por lo que ningún `<option>` coincidía con el `value`/`enabledValue` recibido (`tiendaNombre`), dejando las 9 opciones deshabilitadas y ninguna seleccionada visualmente — no era un problema de propagación de estado (`sucursalDisplayValue`/`sucursalLockValue` ya eran correctos), sino de catálogo de opciones. Corrección (único archivo: `sucursal-select.tsx`): si `value` es una tienda real que no está en `SUCURSALES`, se agrega como opción adicional al render — sin tocar la constante compartida `SUCURSALES` en sí (sigue igual para `PublishModal`/`MasterCalendar`), sin llamadas a Supabase/Repositorios/Servicios. `CoordinatorLayout.tsx`/`RootLayout.tsx`: auditados, sin cambios necesarios. Hallazgo reportado, no corregido (fuera del alcance explícito de esta ronda): `publish-modal.tsx` tiene el mismo bug exacto en su propio `<Select>` de "Sucursal que publica" (misma causa: itera solo `SUCURSALES`). Ver `docs/architecture/frontend/SPRINT_5_2_3_5_SUCURSAL_SELECT_SYNC_REPORT.md` | *(sin rama Git — flujo Git manual del usuario; esta ronda tampoco especificó ninguna)* |
| 6.1 (infraestructura) | 🟡 Auditoría + SQL + Edge Function entregados — pendiente validación del usuario antes del módulo de frontend | Fase 6 — Módulo Administrador, "Gestión de Instaladores". Ante el conflicto real entre "invitar por correo vía Supabase Auth" (exige `service_role`, nunca en el navegador) y "reutilizar la arquitectura existente" (SPA pura, sin backend propio), el usuario eligió construir una Edge Function administrativa reutilizable (`admin-operations`, no exclusiva de instaladores -- encapsula invitar/suspender/reactivar, extensible a futuras operaciones). Antes de tocar la base de datos: auditoría completa de `public.instaladores` (16 columnas, confirmado contra `docs/database/DATABASE_INVENTORY.md`) vs. lo que exige el flujo de invitación -- hallazgos: (1) ninguna columna nueva hace falta, "Pendiente" se deriva de `activo`/`suspendido`/`documentos_ok` ya existentes; (2) el orden del flujo del brief es técnicamente imposible (`instaladores.id` tiene FK real a `auth.users.id` -- hay que invitar primero, crear el registro después, no al revés); (3) RLS de `instaladores` documentada con solo 2 policies de SELECT, ninguna de escritura -- pero como toda escritura administrativa pasa por la Edge Function (`service_role`, se salta RLS), el único hueco real pendiente es una policy de SELECT para `admin` (candidata generada, condicionada a verificación en vivo). Entregado: reporte de auditoría, 2 SQL (verificación + fix condicional, ninguno ejecutado), código completo de la Edge Function + README de despliegue (no desplegada/probada, sin acceso de red desde este entorno). Cero archivos de `src/` tocados -- por instrucción explícita del usuario, el módulo de frontend (repository/hooks/UI) queda para la siguiente ronda, una vez validada esta infraestructura. Hallazgo colateral reportado: `supabase/README.md` documenta un modelo de datos legacy que contradice el modelo real (`docs/database/DATABASE_INVENTORY.md`) -- no se tocó esa carpeta en esta ronda. Ver `docs/architecture/backend/SPRINT_6_1_INSTALADORES_SCHEMA_AUDIT_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 6.1 (diagnóstico 403) | 🟢 Corregido (por lectura de código) — pendiente confirmación del usuario tras redesplegar | Diagnóstico del `403 No autorizado` en `admin-operations`, ya desplegada por el usuario. Evidencia confirmada por el usuario: header `Authorization`/3 secrets/fila real en `admins` -- todo correcto, pero seguía dando `403`. Causa raíz identificada: `verifyCaller()` pasaba el JWT vía `global.headers.Authorization` al crear `callerClient`, pero llamaba a `callerClient.auth.getUser()` SIN el token como argumento -- `global.headers` no alimenta al módulo `auth` (GoTrue), que mantiene sesión interna propia, siempre vacía en un cliente recién creado por invocación. Corrección: extraer el token (`authHeader.replace('Bearer ', '')`) y pasarlo explícitamente a `getUser(token)` (patrón oficial de Supabase, soportado por la librería real, sin necesitar ningún fallback). Se agregó instrumentación completa (`console.log` en cada paso de `verifyCaller()`, JWT nunca logueado completo) para que el usuario confirme contra logs reales tras redesplegar. Único archivo tocado: `supabase/functions/admin-operations/index.ts` (función `verifyCaller()` únicamente) -- ninguna otra lógica del Sprint, arquitectura de doble cliente intacta, cero archivos de `src/`. Ver `docs/architecture/backend/SPRINT_6_1_ADMIN_OPERATIONS_403_DIAGNOSIS_REPORT.md` | *(sin rama Git — flujo Git manual del usuario)* |
| 6.2 | 🟢 Implementado, compilando y fusionado con Sprint 6.1 | Módulo de frontend "Gestión de Instaladores" (`AdminInstaladores`: repository/hooks/UI conectados a datos y a la Edge Function reales), diferido desde el cierre de Sprint 6.1 (infraestructura). Paso 1: `supabase/migrations/0003_service_role_grants_admins_instaladores.sql` (GRANT aditivo `service_role` sobre `admins`/`instaladores`) -- creada, aprobada por el usuario, aplicada vía `apply_migration` y validada contra `information_schema.role_table_grants`. Paso 2: implementación completa -- `src/types/admin-operations.ts` (NUEVO, tipos espejo del contrato de la Edge Function), `src/services/admin-operations.service.ts` (NUEVO, único invocador de `supabase.functions.invoke('admin-operations', ...)`, maneja `FunctionsHttpError.context.json()`), `src/components/shared/admin-instaladores.tsx` (MODIFICADO, mock reemplazado por listado/invitación/suspensión/reactivación reales, estados `isLoading`/`isSending`/`updatingId`/`error` independientes, refresco vía `loadInstaladores()`, sin recarga de página). Lectura estrictamente vía `instaladoresRepository`, escritura estrictamente vía `adminOperationsService` → Edge Function, sin mezclar responsabilidades. Hallazgo de esta ronda: el trabajo de Sprint 6.1 vivía en 2 ramas hermanas nunca fusionadas a esta rama (`supabase/functions/` no existía localmente, docs revertidas a su estado pre-Sprint-6.1) -- con aprobación del usuario se fusionó `docs/sprint-6-1-mcp-documentation` (commit de merge `60ef6d5`), único conflicto real `CLAUDE.md` (resuelto conservando la versión de esta sesión + sección adicional `## Operaciones Git permitidas` de la otra rama). `npm run typecheck`/`npm run build`/`eslint` limpios antes y después del merge. Pendiente: validación funcional real contra Producción (invitar/suspender/reactivar con datos reales -- no ejecutada por disparar un correo real y crear una cuenta real de Auth, efecto no trivialmente reversible sin autorización explícita); `git push` no realizado. Ver `CHANGELOG.md`/`PROJECT_STATUS.md` | `feature/sprint-6-2-instaladores-admin` |
| 6.2 (estabilización) | 🟢 Issues de prioridad ALTA corregidos | Validación funcional real del usuario (invitar/crear/leer instalador) reportó 5 issues. Corregidos los 2 de prioridad ALTA de este Sprint: (1) listado de instaladores vacío -- causa raíz RLS (`pg_policies` sin cobertura para `admin` sobre `instaladores`), corregido con `supabase/migrations/0004_admins_select_instaladores.sql` (nueva policy SELECT, aprobada, aplicada y validada); (2) mensajes de error en inglés -- nueva `translateSupabaseErrorMessage()` (`supabase.service.ts`), integrada en `normalizeSupabaseError()` y en `admin-operations.service.ts`. Backlog documentado sin implementar (instrucción explícita del usuario): enlace de invitación a localhost (`redirectTo`), correo a Spam, plantilla de correo -- todos para un Sprint futuro de autenticación/onboarding. `typecheck`/`build`/`lint` limpios. Ver `CHANGELOG.md`/`PROJECT_STATUS.md` | `feature/sprint-6-2-instaladores-admin` |
| 6.3 | 🟢 Implementado, compilando | Onboarding del Instalador: flujo de aceptar invitación/definir contraseña/primer login/recuperación de contraseña/protección de rutas, sobre la infraestructura de Auth ya existente (Sprint 4.2.1). Auditoría previa encontró gran parte del alcance ya resuelto (login/recuperación-solicitar/logout/guards, todo en español) -- trabajo real acotado a lo faltante: `src/pages/auth/SetPasswordPage.tsx` (NUEVO, pantalla compartida invitación/recuperación en `/nueva-contrasena`, ruta standalone fuera de `ProtectedRoute`/`PublicRoute`); `updatePassword` (`auth.service.ts`/`AuthProvider`); `documentosOk` en `Perfil`/`profile.service.ts`; banner no bloqueante en `RootLayout.tsx` para instalador inactivo/docs pendientes (deliberadamente distinto de `suspendido`, que sí cierra sesión); `CoordinatorOnlyRoute` (`AppRouter.tsx`) hace explícita la protección de `/despacho`/`/trabajos*`. Eliminado `mapLoginError()` duplicado/obsoleto de `LoginPage.tsx` (centralización de traducciones, pedida explícitamente por el brief). Sin tocar Edge Functions/RLS/schema/módulos restringidos. `typecheck`/`build`/`lint` limpios. Documentado para Sprint futuro: `redirectTo` definitivo, branding de correo, SMTP propio, MFA, login social. Ver `CHANGELOG.md`/`PROJECT_STATUS.md` | *(sin rama Git dedicada -- misma rama `feature/sprint-6-2-instaladores-admin`)* |
| 7.1 | 🟢 Implementado, compilando y validado funcionalmente (2 issues bloqueantes detectados y corregidos) | Fase 7 — Publicación de trabajos (Coordinator → Installer): primer flujo funcional, Coordinador crea y publica un trabajo real. Auditoría previa (código + MCP, sin asumir) encontró que la mayor parte ya estaba construida desde Fase 5 (`PublishModal` con validaciones, `onPublish`→`trabajosRepository.create()`) y que las correcciones de RLS/GRANT de Sprint 5.2.2.1/5.2.2.2 (`instalador_fue_notificado()`, `GRANT INSERT,SELECT` a `authenticated` sobre `trabajos`) ya estaban aplicadas en Producción (verificado con `pg_policies`/`role_table_grants`, no como migración formal) -- por eso este Sprint no tocó RLS/migraciones/Edge Functions, los 3 restringidos por el brief. Implementado: bloqueo durante envío/anti-doble-submit (`isSubmitting` + `await onPublish` en `publish-modal.tsx`); actualización inmediata sin recargar en "Mis trabajos" (`TrabajosPage.tsx`, trigger `activeJob?.id`) y en Calendario Maestro (`master-calendar.tsx`, reemplaza el mock `TRABAJOS`/`ESTADO` por `trabajosRepository`/`tiendasRepository`/`trabajoEstadoInfo()` reales, mismo trigger); punto de integración de Realtime documentado (no conectado, para no dejar una implementación incompleta -- infraestructura genérica `useRealtime()` ya existe desde Sprint 4.1.1). "Estado inicial: PENDIENTE" del brief reconciliado con el `DEFAULT 'live'` real (mismo concepto, sin cambio de código, confirmado sin dependencias ocultas en vista/RPC/RLS). **Validación funcional del usuario detectó Issue 1 (bloqueante)**: "Sucursal que publica" aparecía vacío para un Coordinador real (tienda "Multimax Paitilla", ausente de las 9 `SUCURSALES` legacy) -- mismo bug ya documentado sin corregir desde Sprint 5.2.3.5, no una regresión de este Sprint; corregido con el mismo patrón ya validado en `sucursal-select.tsx` (unión `SUCURSALES` + valor real). Observación de arquitectura no bloqueante del usuario (Admin publicando trabajos) documentada para Sprint futuro, sin implementar. **2ª validación funcional detectó un nuevo bloqueante**: "Calendario Maestro" no muestra trabajos para el Administrador real -- causa raíz confirmada vía MCP: ninguna policy de `trabajos` reconoce a `public.admins` (todas exigen `auth.uid()` en `coordinadores`), RLS devuelve `[]` para el 100% de los trabajos bajo esa sesión, no un problema de refetch/fecha. `supabase/migrations/0005_admins_select_trabajos.sql` (mismo patrón que la 0004) -- verificada contra los 4 requisitos del usuario (solo SELECT, sin tocar policies existentes, sin INSERT/UPDATE/DELETE, mismo patrón que 0004), aprobada y aplicada. Validación funcional post-aplicación con simulación real de RLS (`SET LOCAL ROLE`/`request.jwt.claims`): Administrador Principal pasa de 0 a 9 trabajos visibles (incluido el recién publicado); Coordinador Test sigue viendo exactamente los 4 de su propia tienda, sin regresión. `typecheck`/`build`/`lint` limpios. Sin `git push`/PR/merge. Ver `CHANGELOG.md`/`PROJECT_STATUS.md` | `feature/sprint-7-publicacion-trabajos` |
| 7.2 | 🟢 Implementado, compilando y validado técnicamente -- pendiente validación funcional del usuario | Fase 7 — Notificación a instaladores elegibles y envío de ofertas: cierra el ciclo "Coordinador publica → instaladores elegibles notificados → instalador envía oferta → Coordinador ve las ofertas". Precedido por varias rondas de auditoría exclusivamente funcional (sin código) donde el usuario confirmó formalmente: regla de elegibilidad (`activo=true AND suspendido=false AND` misma empresa/provincia/zona, sin geolocalización/PostGIS/Google Maps); mecanismo de notificación (RPC atómico `notificar_instaladores_elegibles`, no un loop del cliente); comportamiento ante 0 instaladores elegibles (NO es un error técnico -- el trabajo queda publicado igual, Toast informativo, sin rollback/eliminación); patrón de UI del detalle del Instalador (maestro-detalle interno, sin ruta/página nueva, mismo criterio que `MasterCalendar`). Implementado: `supabase/migrations/0006_notificar_instaladores_elegibles.sql` (NUEVO, aprobada y aplicada) -- función `SECURITY INVOKER` (mismo criterio que `submit_bid`/`asignar_instalador`), sin GRANT/policy nuevos (las policies ya existentes del Coordinador cubren todo), idempotente vía la restricción real `UNIQUE(trabajo_id, instalador_id)` ya existente + `ON CONFLICT DO NOTHING` (verificado contra `pg_constraint`, no asumido) -- regla de idempotencia documentada explícitamente en `CHANGELOG.md`; wrapper tipado `callNotificarInstaladoresElegibles` (`database.service.ts`) integrado en `onPublish` (`CoordinatorLayout.tsx`, Toast `'info'` para 0 elegibles vs. `'error'` para fallo real del RPC); `trabajos-para-instalador.repository.ts` (NUEVO, lectura de la vista `trabajos_para_instalador`, ya filtrada por RLS a `auth.uid()`, sin implementar `Repository<T>` por ser de solo lectura); `installer-solicitudes.tsx` (NUEVO) reemplaza el estado vacío fijo de "Solicitudes" del Instalador por el listado real + detalle + formulario de oferta (`submit_bid`, ya existente), reutilizando `.mx-myjobs`/`.mx-myjob*` (Sprint 3.12) sin inventar convención visual nueva; `responses-panel.tsx` reemplaza el estado vacío fijo (Sprint 5.1.3) por las ofertas reales del `activeJob` (`ofertasRepository.getByTrabajoId`), nombre del instalador resuelto vía mapa `instaladoresRepository.getByEmpresaId` (mismo patrón que `tienda_id` en `master-calendar.tsx`); `job-summary-card.tsx` agrega `trabajoId?: string` (opcional, retrocompatible con el job de demostración de `DespachoPage.tsx`) para exponer el uuid real del trabajo. `typecheck`/`build`/`lint` limpios (mismos 3 warnings preexistentes, sin advertencias nuevas). Sin `git commit`/`push`/PR/merge -- pendientes de aprobación explícita del usuario. Ver `CHANGELOG.md`/`PROJECT_STATUS.md` para el detalle completo, incluidos los 13 criterios de aceptación oficiales de este Sprint | *(misma rama `feature/sprint-6-2-instaladores-admin`)* |
| 7.2 (fix GRANT + UX) | 🟢 Incidente de privilegios SQL corregido y validado extremo a extremo; mejora de UX agregada | Continuación del Sprint 7.2 tras la primera validación funcional real del usuario. **Incidente 1**: `notificar_instaladores_elegibles()` fallaba con `42501 permission denied for table trabajo_instaladores` y `ResponsesPanel` con `42501 permission denied for table ofertas` -- diagnóstico vía MCP (simulación real de sesión, `SET LOCAL ROLE`/`request.jwt.claims`) confirmó causa raíz idéntica al Sprint 5.2.2.2: privilegios SQL (`GRANT`) faltantes para `authenticated`, no RLS (las policies ya existentes eran correctas y nunca llegaban a evaluarse). Corregido con `supabase/migrations/0007_authenticated_grants_sprint72.sql` (`GRANT SELECT,INSERT,UPDATE` sobre `trabajo_instaladores`, `GRANT SELECT,INSERT` sobre `ofertas`). **Incidente 2** (detectado en la validación extremo a extremo posterior): el Instalador no podía leer sus propias solicitudes -- `42501 permission denied for view trabajos_para_instalador`, mismo tipo de causa sobre un tercer objeto (una vista, con `GRANT` independiente de sus tablas subyacentes). Corregido con `supabase/migrations/0008_authenticated_view_grants_sprint72.sql` (`GRANT SELECT` sobre la vista). Ambas migraciones aditivas, sin tocar RLS/RPC/frontend/repositorios/servicios, siguiendo exactamente el patrón ya validado de Sprint 5.2.2.2. Validación funcional extremo a extremo con datos reales persistidos (publicar → notificar → `trabajo_instaladores` → ver solicitud → `submit_bid` → `ofertas` → `ResponsesPanel`), sin regresión en Sprint 7.1/7.2 (16 policies RLS verificadas idénticas antes/después); datos de prueba (`trabajo` `SPRINT72-TEST-VALIDACION` y registros relacionados) eliminados al cierre. **Hallazgo posterior, sin corrección de datos** (reportado por el usuario tras una prueba real en el navegador con 2 trabajos publicados para "Parque Lefevre"): diagnóstico confirmó que el RPC y las migraciones funcionaban correctamente (registro real creado en `trabajo_instaladores` para el instalador elegible, sin diferencias de texto en `empresa_id`/`provincia`/`zona`, verificado byte a byte) -- la ausencia percibida de "Toast de notificación" se debía a que nunca existió una rama de Toast para el caso de éxito (solo error/cero-elegibles); la ausencia percibida de "el instalador no recibió la solicitud" se explica porque la cuenta del instalador elegible nunca completó el onboarding (`auth.users.confirmed_at`/`last_sign_in_at` ambos `null` en el momento de la prueba), no por un fallo del pipeline. **Mejora de UX implementada** (autorizada explícitamente, alcance acotado): `src/layouts/CoordinatorLayout.tsx` agrega una rama de Toast `'info'` para `notificacionResult.data > 0` ("Se notificó a N instalador(es) elegible(s)."), construida únicamente con el valor ya devuelto por el RPC, sin consultas adicionales -- sin tocar RPC/RLS/migraciones/repositorios/servicios/`ResponsesPanel`/`InstallerDashboard`. `typecheck`/`build`/`lint` limpios en cada ronda. Sin `git commit`/`push`/PR/merge. Ver `CHANGELOG.md`/`PROJECT_STATUS.md` | *(misma rama `feature/sprint-6-2-instaladores-admin`)* |
| 7.3 | 🟢 Implementado, compilando | Módulo de Cuenta de Usuario (Perfil, Configuración, Seguridad) -- Sprint 7.2 congelado explícitamente (verificado: cero archivos de publicación de trabajos/`ResponsesPanel`/instaladores/RPC/Supabase/Edge Functions/autenticación existente tocados). Implementa las 3 pantallas del menú de usuario (`HeaderUserMenu`, `disabled` sin destino real desde el Sprint 4.2.1): "Mi perfil" (`/perfil`), "Configuración" (`/configuracion`), "Cambiar contraseña" (`/cambiar-contrasena`). Decisión arquitectónica: como `RootLayout.tsx` no monta `<Outlet/>` para `instalador`/`admin` fuera de "Modo Coordinador", las 3 rutas se declaran hermanas de `/` (no hijas de `RootLayout`), con layout propio y autocontenido `AccountLayout.tsx` (NUEVO) -- resuelve `profile`/`onLogout` vía `useAuth()`, reutiliza `Header`/`Footer`/`MxSubtabs`/`MxSubtabButton` sin modificarlos; cero cambios en `RootLayout.tsx`/`CoordinatorLayout.tsx`. "Mi Perfil" (`ProfilePage.tsx`, NUEVO) muestra datos 100% reales -- `Perfil` extendido con `telefono`/`provincia`/`zona`/`creadoEn`/`instaladorInfo` (`types/perfil.ts`/`profile.service.ts`, sin ninguna consulta nueva a Supabase, esos campos ya viajaban en la fila que `resolveProfile()` consultaba), "No disponible" para todo campo sin valor real, botón "Editar perfil" preparado (`disabled`). "Configuración" (`SettingsPage.tsx`, NUEVO) implementa de forma honesta -- persistencia real en `localStorage` (`useUserPreferences`, NUEVO hook), sin tabla en Supabase -- las preferencias que el brief pidió funcionales (Notificaciones/Sonidos/Confirmaciones/Recordar sucursal/Vista inicial/Mostrar ayudas), documentando que ninguna está conectada aún a otro módulo; Tema/Idioma/Sesiones activas deshabilitados con badge "Próximamente" (pedido explícito del brief); Último login/Dispositivo como información real de solo lectura. "Cambiar contraseña" (`ChangePasswordPage.tsx`, NUEVO) reautentica con la contraseña actual (`useAuth().login()`) antes de `useAuth().updatePassword()` -- ambos ya existentes, APIs oficiales de Supabase, sin implementación propia -- con `PasswordStrengthMeter` (NUEVO, reutiliza `Progress`) y validaciones en tiempo real (`lib/password-strength.ts`, NUEVO, funciones puras). `lib/perfil-format.ts` (NUEVO) centraliza `ROL_LABEL`/`ESTADO_LABEL`/`initialsFrom`, antes duplicados en `header-user-menu.tsx` -- ese componente ahora los importa de ahí; único cambio funcional en ese archivo (área de "autenticación existente"): los 3 ítems de menú dejan de estar `disabled` y navegan a las rutas nuevas, sin tocar lógica de sesión/JWT/RLS. `typecheck`/`build`/`lint` limpios (mismos 3 warnings preexistentes, sin advertencias nuevas). Sin `git commit`/`push`/PR. Ver `CHANGELOG.md`/`PROJECT_STATUS.md`/`ARCHITECTURE.md` §14.11 | *(misma rama `feature/sprint-6-2-instaladores-admin`)* |

| 8.1 | 🟢 Implementado, compilando — DETENIDO PARA REVISIÓN antes del Sprint 8.2 | Fase 8 — "Evolución del BackOffice de Administración", Dashboard Ejecutivo. Auditoría previa vía MCP (`information_schema.columns`/`pg_policies` sobre `trabajos`/`trabajo_instaladores`/`instaladores`) determinó qué KPIs son calculables: 6 de los 8 mínimos pedidos (publicados hoy/activos/pendientes/finalizados/cancelados/instaladores activos) reales, sobre `trabajos`/`instaladores` (RLS ya scoped a la empresa del admin, mismo patrón que `MasterCalendar`/`AdminInstaladores`). Los otros 2 quedan "No disponible", con causa raíz documentada: "Tiempo promedio de respuesta" bloqueado por un hueco de RLS real (`trabajo_instaladores` sin policy `SELECT` para `admin`, mismo patrón de hueco ya corregido 3 veces antes para otros objetos, no corregido acá por estar fuera del alcance de este Sprint -- el `GRANT` de tabla sí existe, así que la consulta devolvería `0` filas silenciosamente en vez de fallar, por eso se optó por no ejecutarla y declarar el indicador explícitamente en vez de fabricar un promedio de un conjunto vacío); "Tiempo promedio de instalación" estructuralmente imposible (`trabajos` no tiene ninguna columna de timestamp de finalización). Implementado: `src/services/admin-dashboard.service.ts` (NUEVO, `getAdminKpis()`, incluye `calcularTiempoPromedioRespuestaMin()` ya listo para activarse en cuanto exista la policy faltante); `src/components/shared/admin-kpi-dashboard.tsx` (NUEVO, reutiliza `Card`/`CardHeader`/`StatGrid`/`StatTile`/`Loading`, cero componentes/CSS nuevos); `admin-panel.tsx` (MODIFICADO, nueva pestaña "Dashboard" activa por defecto, antes de "Calendario maestro"/"Instaladores", mismo patrón `MxSubtabs`). Requisito adicional del brief: eliminada la pestaña "Instalador" de `AdminVistaSwitch` (`admin-vista-switch.tsx`, MODIFICADO) -- se retiró solo la opción del array renderizado, sin angostar el tipo `AdminVista`, para no forzar ningún cambio en `RootLayout.tsx` (explícitamente restringido este Sprint, junto con Auth/Login/`redirectTo`/Sprint 7.2/Sprint 7.3/`CoordinatorLayout.tsx`/publicación de trabajos/`ResponsesPanel`/instaladores-flujo-operativo/Edge Functions/RLS/RPC/migraciones existentes -- verificado con `git status` que ninguno fue tocado). `typecheck`/`build`/`lint` limpios (mismos 3 warnings preexistentes, sin advertencias nuevas). Sin `git commit`/`push`. Ver `CHANGELOG.md`/`PROJECT_STATUS.md` | *(misma rama `feature/sprint-6-2-instaladores-admin`)* |
| 8.1.1 | 🟢 Implementado, compilando — DETENIDO PARA REVISIÓN antes del Sprint 8.2 | Refinamiento del Dashboard Ejecutivo, continuación exclusiva del Sprint 8.1 (mismas restricciones, ninguna tocada). 4 ajustes: (1) `AdminKpiDashboard` envuelto en `PageContainer` -- corrige el espaciado insuficiente respecto a la barra de tabs; (2) eliminados por completo los mensajes técnicos que el Sprint 8.1 mostraba para los indicadores no disponibles (nombres reales de tabla/columna/RLS, `...Motivo`) -- esa información ahora vive exclusivamente en `ARCHITECTURE.md` §14.14, nunca en la interfaz (nueva regla permanente en `CLAUDE.md`: "ningún indicador debe mostrar jamás mensajes técnicos"); (3) `src/components/shared/admin-kpi-card.tsx` (NUEVO) -- `AdminKpiCard`, componente único para los 8 KPIs, 4 estados nativos (`ready`/`loading`/`pending`/`error`), reutiliza las mismas clases CSS que `Counter`/`StatTile` (`.mx-stat-v`/`.mx-stat-l`/`.mx-stat-s`, sin clases nuevas) + `Skeleton`/`Badge` (ya existentes, sin consumidor real hasta ahora); (4) `admin-dashboard.service.ts` (MODIFICADO) reescrito en 3 capas explícitas -- obtención (repositorios) → transformación (funciones puras, `AdminKpiData`) → presentación (`buildAdminKpiViewModels()`, NUEVO, único punto que decide `status`/orden/etiquetas) -- arquitectura preparada para KPIs futuros (Sprints 8.2/8.4/8.6/8.7/8.8/8.9) vía un patrón de 3 pasos documentado en el propio archivo, sin infraestructura especulativa. `useMemo` sobre el cálculo de los view models. `typecheck`/`build`/`lint` limpios (mismos 3 warnings preexistentes, sin advertencias nuevas). Sin `git commit`/`push`. Ver `CHANGELOG.md`/`PROJECT_STATUS.md`/`ARCHITECTURE.md` §14.14 | *(misma rama `feature/sprint-6-2-instaladores-admin`)* |
| 8.2 | 🟢 Implementado, compilando — DETENIDO PARA REVISIÓN antes del Sprint 8.3 | Master Calendar (Fase 1), segundo sub-sprint de la Fase 8, exclusivamente sobre `MasterCalendar` (mismas restricciones que 8.1/8.1.1, ninguna tocada -- verificado con `git status`). Arquitectura en capas nueva: `types/calendar.ts` + `services/calendar.service.ts` (obtención → transformación, mismo criterio de 3 capas del 8.1.1) + `hooks/useCalendarData.ts` (única fuente de estado del módulo: mes visible, filtros persistentes entre meses, día seleccionado) + 5 componentes presentacionales (`CalendarFilterBar`/`CalendarDayIndicators`/`CalendarJobCard`/`CalendarDayDrawer`/`CalendarLegend`, todos NUEVOS); `MasterCalendar` reescrito como orquestador delgado. Rendimiento: `trabajosRepository.getByMonthAndFilters()` (NUEVO, aditivo) reemplaza el `getAll()` + filtrado 100% en cliente usado desde el Sprint 7.1 -- una sola consulta server-side por mes+filtros; abrir el Drawer de un día no dispara ningún request adicional (`getDayJobs()`, función pura sobre el mes ya en memoria). Drawer lateral: `ui/drawer.tsx` (Fase 3) era en realidad un bottom-sheet -- se agregó `variant?: 'sheet' | 'lateral'` (default `'sheet'`, cero cambio para `PublishModal`) en vez de duplicar el componente, con CSS nuevo mínimo y aditivo. Acciones rápidas: 5 botones `disabled` por trabajo con `Tooltip` (primer consumidor real de ese componente, sin uso previo en el proyecto), preparados para un Sprint futuro, sin lógica conectada. Interpretaciones de datos documentadas, ninguna inventada: Prioridad = 2 niveles reales (`urgente boolean`, no 3); "vencido"/"crítico" derivados de `estado`/`fecha`/`urgente` (no existen esas columnas); "Empresa instaladora" = `empresas` hoy (la entidad distinta la introducirá el Sprint 8.3); "Tiempo estimado"/"Tiempo real" siempre "No disponible" (mismo hallazgo ya documentado en el Sprint 8.1). Estados uniformes a nivel de módulo (`loading`/`ready`/`error`) -- error siempre "No fue posible cargar la información.", nunca el mensaje real de Supabase. Se preservó el trigger `activeJob?.id` (`useOperationalContext()`) que el `MasterCalendar` anterior ya usaba desde el Sprint 7.1 para refrescarse tras publicar, sin recargar la página. Auditoría de duplicación: `CalendarFilterBar` reutiliza `Select` en vez del `<select>` con estilos en línea que el filtro de sucursal anterior usaba (corrige una inconsistencia preexistente); `hoyComoTexto()` (`dashboard.service.ts`, antes privada) se exportó y se reutiliza tal cual desde `calendar.service.ts`. Sin componentes/CSS/helpers/hooks/tipos duplicados detectados. `typecheck`/`build`/`lint` limpios (mismos 3 warnings preexistentes, sin advertencias nuevas -- se detectó y corrigió un `eslint-disable` sobrante en `useCalendarData.ts` durante el desarrollo). Sin `git commit`/`push`. Ver `CHANGELOG.md`/`PROJECT_STATUS.md`/`ARCHITECTURE.md` §14.15 | *(misma rama `feature/sprint-6-2-instaladores-admin`)* |
| A (Gestión de Administradores y Coordinadores) — Sprint A "Fundamentos de schema" | 🟢 Aplicado en Producción vía MCP, validado — DETENIDO PARA REVISIÓN antes del Sprint B | Precede al análisis técnico `ANALISIS_GESTION_USUARIOS.md` (sección "CIERRE ARQUITECTÓNICO", C1/C3, aprobada explícitamente por el usuario) para el nuevo módulo "Gestión de usuarios" (Administradores + Coordinadores, `AdminPanel`). Sprint A es exclusivamente el schema base de la distinción Administrador Principal/Secundario -- sin `admin-operations`, sin RLS de listado, sin frontend, sin Coordinadores (todo eso queda para Sprints B en adelante). Migraciones aplicadas vía MCP: `0011_admins_es_principal.sql` (`admins.es_principal boolean not null default false` + índice único parcial `uq_admins_un_principal_por_empresa` `on admins(empresa_id) where es_principal` + backfill genérico `distinct on (empresa_id) ... order by created_at asc`, sin hardcodear ningún id -- precheck confirmó 1 empresa/1 admin real en Producción, backfill mostrado y confirmado por el usuario antes de aplicarse, resultado: `arnulfo.mendieta@multimax.net` quedó `es_principal=true`); `0012_proteger_ultimo_admin_principal.sql` (función `security definer` + trigger `before update or delete on admins for each row`, bloquea `DELETE`/`activo=false`/`es_principal=false` sobre el único Principal activo de una empresa, incluida la cascada real desde `auth.users` vía `ON DELETE CASCADE`). 9 pruebas de seguridad ejecutadas contra Producción real dentro de transacciones con `ROLLBACK` explícito (sin dejar datos de prueba permanentes, sin crear un segundo Principal permanente) -- las 9 en PASS, incluida la prueba de scoping por `empresa_id` (empresa de prueba temporal con su propio Principal, protección independiente confirmada) y la de atomicidad (estado real verificado sin cambios después de cada prueba fallida). `typecheck`/`build` limpios, sin ningún archivo de aplicación tocado. Ver `ANALISIS_GESTION_USUARIOS.md` para el análisis completo y el detalle de cada prueba | *(misma rama `feature/sprint-9-1-nginx-production`, sin `git commit`/`push`)* |
| B+C (Gestión de Administradores y Coordinadores) — Sprint B "Backend" + Sprint C "Contrato frontend/backend" | 🟢 Desplegado y validado extremo a extremo en Producción — DETENIDO PARA REVISIÓN antes del Sprint D | **Sprint B**: `admin-operations` extendida (v6→v7, desplegada vía MCP) con `invite_admin`/`set_admin_activo` -- gate `caller.activo && caller.es_principal===true`, `empresa_id`/`es_principal`/`activo` siempre server-side (nunca del payload), rechazo explícito sobre el Principal antes de cualquier `UPDATE`. Migración `0013_admins_select_misma_empresa.sql` (policy RLS `SELECT` "admins ven administradores de su empresa", acotada por `empresa_id`) causó `42P17 infinite recursion` real en Producción (subconsulta contra la misma tabla que protegía) -- **rompía el login de cualquier admin real** (incluido el Principal); corregida de inmediato con `0014_fix_admins_select_recursion.sql` (función `security definer` `es_admin_de_empresa()`, mismo patrón que `instalador_fue_notificado()`), validada (login restaurado, roster visible, aislamiento por empresa confirmado). 21 pruebas SQL (`invite_admin`/`set_admin_activo`, dentro de `BEGIN...ROLLBACK`) todas PASS antes del deploy. **Diagnóstico y cierre del problema de `redirect_to`**: el enlace de invitación llegaba a `https://instalaciones.multimax.net/` en vez de `/nueva-contrasena` -- código de `inviteAdmin()`/`inviteInstalador()` confirmado correcto (concatena `/nueva-contrasena` cuando existe el secret `APP_URL`); causa real de **configuración** (secret `APP_URL` + Redirect URLs de Supabase Auth), corregida por el usuario fuera de este entorno, sin cambios de código. Flujo extremo a extremo confirmado por el usuario: invitación → correo correcto → `/nueva-contrasena` → contraseña inicial → Dashboard → logout → login → Dashboard (beneficia también a `invite_instalador`, mismo secret). **Sprint C**: `database.generated.ts` regenerado vía MCP (desactualizado desde el Sprint A, sin `es_principal`); `InviteAdminPayload`/`SetAdminActivoPayload` (`types/admin-operations.ts`) + `inviteAdmin`/`setAdminActivo` (`services/admin-operations.service.ts`), mismo patrón que instaladores -- sin nada para Coordinadores (backend/RLS/GRANTs inexistentes, verificado sin cambios). Sin componentes/pantallas CRUD. `lint`/`typecheck`/`build`/`dev` verificados limpios (mismos 3 warnings preexistentes). Sin `git commit`/`push` | *(misma rama `feature/sprint-9-1-nginx-production`, sin `git commit`/`push`)* |
| Módulo CRUD de Administradores (Gestión de Administradores y Coordinadores) | 🟢 Implementado, compilando — DETENIDO PARA REVISIÓN, pendiente validación visual | Primera UI real del módulo -- quinta pestaña "Administradores" en `AdminPanel` (ícono `ShieldCheck`), usando exclusivamente los contratos ya validados en el Sprint C (`adminsRepository.getByEmpresaId()`, `adminOperationsService.inviteAdmin`/`.setAdminActivo`) -- sin tocar RLS/migraciones/Edge Functions. `src/components/shared/admin-administradores.tsx` (NUEVO) -- mismo patrón estructural/visual que `admin-instaladores.tsx` (tabla + formulario de invitación + toggle de estado + Toast local, clases `.mx-admintable`/`.mx-adminrow*`/`.mx-admin-act`/`.mx-invite*` ya existentes, sin CSS nuevo). Badge "Principal"/"Secundario" + "Activo"/"Inactivo" por fila; "Invitar"/"Activar-desactivar" gateados en UI por `profile.esPrincipal === true` (matriz de permisos aprobada, C2) -- únicamente UX, la autorización real vive en `admin-operations` (rechaza con `403` igual, con o sin el botón visible); la fila del propio Principal nunca muestra el botón de activar/desactivar. **Gap cerrado**: `Perfil`/`profile.service.ts` no exponían `es_principal` -- se agregó `esPrincipal: boolean | null` (rama `admins`, sin query nueva; `null` para `coordinador`/`instalador`). Sin pestaña de Coordinadores (backend inexistente, Sprint D pendiente). `lint`/`typecheck`/`build` limpios (mismos 3 warnings preexistentes). **Sin validación visual en navegador** (este entorno de trabajo no tiene acceso a un navegador) -- pendiente que el usuario la confirme antes de cerrar el módulo por completo. Sin `git commit`/`push` | *(misma rama `feature/sprint-9-1-nginx-production`, sin `git commit`/`push`)* |
| 9.2 (Backend / operaciones de administradores) — "Consolidación y verificación" | 🟢 Verificado, sin brechas — DETENIDO PARA REVISIÓN | **Sin funcionalidad nueva** -- Fase 1 (análisis obligatorio, aprobada antes de escribir código) concluyó que las 7 operaciones/protecciones mínimas pedidas por este Sprint (invitar/activar-desactivar Secundario, consultar, proteger Principal, proteger último Principal, aislar por empresa, autorizar por rol) ya estaban implementadas y desplegadas desde los Sprints A/B/C (`admin-operations` v7) -- confirmado leyendo en fresco los 4 archivos centrales (`admin-administradores.tsx`/`admin-panel.tsx`/`admin-operations.service.ts`/`admin-operations/index.ts`), no asumido. Fase 2 = exclusivamente verificación/auditoría/documentación, cero cambios de código de aplicación (confirmado con `git status`/`git diff`, ambos vacíos para archivos de `src/`/`supabase/functions/`). 9 pruebas técnicas re-ejecutadas con evidencia fresca (dentro de `BEGIN...ROLLBACK` cuando implicaban escritura, `0` datos de prueba persistidos, usando los 2 administradores secundarios reales ya existentes de rondas anteriores para el gate de autorización): Principal ejecuta operaciones, Secundario no puede, usuario no-admin rechazado, aislamiento entre empresas, protección del último Principal y del Principal (trigger `23514`), operación exitosa (`toggle` real revertido), manejo de errores (revisión de código), sin credenciales privilegiadas en `dist/assets/*.js` (auditado, único JWT presente = clave `anon` pública). RLS (`pg_policies`)/GRANTs (`authenticated` solo `SELECT`)/trigger (`pg_get_functiondef`) re-auditados vía MCP, idénticos a `0011`-`0014`, sin cambios. `lint`/`typecheck`/`build` limpios (mismos 3 warnings preexistentes). Sin script `test` en `package.json` (documentado, no inventado). Sin migraciones nuevas, sin cambios de Docker/Nginx/Traefik. Sin `git commit`/`push` | *(rama `feature/sprint-9-2-admin-operations`, sin `git commit`/`push`)* |
| 9.3 (Gestión de Coordinadores) | 🟢 Implementado, compilando — DETENIDO PARA REVISIÓN | Cierra la parte de Coordinadores del módulo "Gestión de usuarios" (pendiente desde el Módulo CRUD de Administradores, antes referida como "Sprint D" en `ANALISIS_GESTION_USUARIOS.md`). Traslada al Admin la gestión de coordinadores de tienda, mismo patrón exacto que Administradores -- misma Edge Function `admin-operations` (sin crear una segunda), mismo `inviteUserByEmail()` + `redirectTo: ${APP_URL}/nueva-contrasena`, mismo rollback `deleteUser()`. **Base de datos** (aplicada en Producción vía MCP antes de esta ronda, verificada en vivo sin discrepancias): `0015_coordinadores_service_role_grant.sql` (GRANT `service_role` sobre `coordinadores`, mismo molde que `0003`) + `0016_admins_select_coordinadores.sql` (policy RLS `"admins ven coordinadores de su empresa"`, reutiliza `es_admin_de_empresa()` de `0014`). **Edge Function** (extendida, desplegada): `invite_coordinador` (valida `tienda_id` contra la empresa del caller server-side, `rol` fijo `'coordinador'`, `activo:true`, rollback si falla el `INSERT`), `set_coordinador_activo` (mismo patrón que `setAdminActivo()`, sin gate de `es_principal` -- matriz C2: gestionar coordinadores es igual para Principal/Secundario), `list_coordinadores` (decisión técnica nueva: `coordinadores` no tiene columna `email` -- se resuelve vía Auth Admin API `getUserById()` dentro de la misma Edge Function, sin agregar columna ni exponer `auth.users` al cliente). **Frontend**: `admin-coordinadores.tsx` (NUEVO, mismo patrón que `admin-administradores.tsx` + `<Select>` de tienda vía `tiendasRepository.getByEmpresaId()`, ya existente); `admin-operations.service.ts`/`types/admin-operations.ts` extendidos; sexta pestaña "Coordinadores" en `admin-panel.tsx`. Repositorios sin cambios (`coordinadoresRepository`/`tiendasRepository` ya tenían lo necesario). Sin edición ni reenvío de invitación (Administradores tampoco los tiene -- MVP: listar/invitar/activar-desactivar). `typecheck`/`build`/`lint` limpios (mismos 3 warnings preexistentes). **Sin invitación real enviada** (sin correo de prueba autorizado en este entorno) -- pendiente de validación funcional real del usuario. Sin `git commit`/`push` | *(rama `feature/sprint-9-3-coordinators`, sin `git commit`/`push`)* |
| Ajustes funcionales del flujo Instalador (aprobación + empresa + solicitudes por zona + Mis trabajos) | 🟢 Implementado, compilando — DETENIDO PARA REVISIÓN | Auditoría previa (MCP + código) confirmó 6 gaps reales: sin acción para activar (`activo`) a un instalador tras la invitación; header del portal con texto hardcodeado (ignoraba la empresa instaladora real); "Solicitudes" acotada a la zona del instalador (RLS dependiente de `trabajo_instaladores`, poblada solo por `notificar_instaladores_elegibles()`, filtrada por zona); badge "Oferta enviada" incorrecto para trabajos de otra zona; "Mis trabajos" sin categoría "Ofertados"; ninguna UI de asignación en todo el proyecto (`callAsignarInstalador` sin consumidor real) ni policy RLS de `UPDATE` para `admins` sobre `trabajos`/`trabajo_instaladores`. **Migraciones nuevas, NO aplicadas**: `0018_instaladores_visibilidad_ampliada.sql` (policy SELECT aditiva -- instaladores activos ven trabajos `'live'` de su empresa en cualquier zona -- + `trabajos_para_instalador` redefinida con LEFT JOINs, expone `oferta_enviada` real); `0019_admins_asignan_trabajos.sql` (2 policies UPDATE nuevas para `admins`, espejo de las de `coordinadores`). **Edge Function extendida, no desplegada**: `set_instalador_activo`. **Frontend**: `admin-instaladores.tsx` (3 acciones según estado real), `useEmpresaInstaladoraNombre` (hook nuevo), `phone-frame.tsx`/`installer-dashboard.tsx` (empresa real en encabezado), `installer-solicitudes.tsx` (badge de zona + oferta corregida), `installer-jobs.tsx` (4 categorías con chips), `responses-panel.tsx` (botón "Asignar" real). `typecheck`/`build`/`lint` limpios (mismos 3 warnings preexistentes). Sin migraciones aplicadas, sin Edge Function desplegada, sin `git push` | `feature/instaladores-flujo-completo` |
| 9.4 (Dashboard Ejecutivo — estadísticas operativas) | 🟢 Implementado, compilando — DETENIDO PARA REVISIÓN | Extiende (sin rediseñar) el Dashboard Ejecutivo con una segunda capa de estadísticas, tomando como referencia de producto el panel administrativo del prototipo InstalaMax -- sin copia visual, sin reemplazar ninguno de los 8 KPIs existentes. Auditoría previa obligatoria (Fase 1/2, MCP `list_tables`/`pg_policies` sobre las 9 tablas reales de Producción) contra las 9 métricas del prototipo: solo el rango de fechas (`publicado_at`, decisión confirmada con el usuario), filtro de sucursal, resumen por estado y desgloses por sucursal/instalador (todo por conteo) resultaron IMPLEMENTABLE AHORA. Las 6 métricas financieras/de vendedor (ingresos, comisión Multimax 30%, pagado a instaladores, pagado a vendedores, utilidad neta, cobros extra pendientes) quedaron NO IMPLEMENTABLE con causa raíz verificada cada una: sin fuente oficial que defina el 30% (`grep` exhaustivo sin resultados reales); `admins` sin ninguna policy RLS de SELECT sobre `ofertas`/`trabajo_instaladores` (mismo patrón de bug ya corregido para `trabajos` en `0005`, sin aplicar ningún cambio de RLS en este Sprint); el concepto "vendedor" no existe en el modelo de datos; no existe tabla de cobros adicionales. **Archivos**: `trabajos.repository.ts` (MODIFICADO, +`getByRangoPublicado`); `types/admin-dashboard-stats.ts` (NUEVO); `services/admin-dashboard-stats.service.ts` (NUEVO, 1 sola consulta agregada, sin N+1); `components/shared/admin-dashboard-stats.tsx` (NUEVO, reutiliza `Card`/`StatGrid`/`Select`/`Input`/`Button`/`Badge`/`.mx-admintable`/`.mx-adminrow*` ya existentes, sin CSS nuevo); `admin-kpi-dashboard.tsx` (MODIFICADO, +1 import +1 línea de render, debajo del `StatGrid` existente). Validación de datos contra Producción real vía MCP (solo lectura) confirma que la lógica reproduce exactamente los datos reales. `typecheck`/`build`/`lint` limpios (mismos 3 warnings preexistentes). Sin migraciones, sin cambios de RLS. Sin `git push`/merge | *(rama `feature/sprint-9-4-dashboard-statistics`)* |

> **Actualización de metodología documental (2026-07-22)**: a partir de la Fase 5, este archivo queda formalizado como el **índice maestro** del proyecto (todas las fases, todos los Sprints, orden cronológico, estado, dependencias, roadmap) y reemplaza por completo a los futuros archivos `PHASE_X.md` — no se crean `PHASE_5.md`/`PHASE_6.md`/etc. Los `PHASE_1.md`–`PHASE_4.md` existentes quedan como registro histórico de las fases que ya documentan, no como plantilla para las siguientes. Ver `PROJECT_STATUS.md` (sección "Actualización de metodología documental") para el detalle completo de esta decisión.

## Estado real del proyecto (resumen, actualizado 2026-07-10)

- **Bloques HTML migrados y visibles en la aplicación**: Header (`mx-top`, Sprint 3.1, ✅), `mx-suc-sel` (Sprint 3.4, ✅ — visible en `RootLayout` cuando `role === 'coordinador'`), `mx-subtabs` (Sprint 3.3, ✅ — visible en `RootLayout` cuando `role === 'coordinador'`, en ese orden: `mx-suc-sel` antes de `mx-subtabs`, igual que en el HTML fuente), `PublishModal` (Sprint 3.5, ✅ — visible en `RootLayout` como hermano de los bloques de `role`, ahora abierto mediante el botón real de `CoordinatorEmptyState` en vez de forzado; validación real y visual aprobadas por el usuario), `CoordinatorEmptyState` (Sprint 3.6, ✅ — estado vacío de `Coordinator`/`mx-qempty`, visible en `RootLayout` después de `mx-subtabs` cuando `role === 'coordinador'`; validación real y visual aprobadas por el usuario, incluida la apertura de `PublishModal` desde el botón "Publicar trabajo").
- **Bloques HTML migrados y visibles en la aplicación (continuación)**: `Radar` (Sprint 3.7, ✅ — panel SVG de instaladores, visible en `RootLayout` como último hijo del bloque `role === 'coordinador'`, con datos mock de demostración; validación real y visual aprobadas por el usuario).
- **Bloques HTML migrados y visibles en la aplicación (continuación)**: `CountRing` (Sprint 3.8, ✅ — anillo SVG de countdown, visible en `RootLayout` dentro del bloque `role === 'instalador'`, con props mock estáticas; validación real y visual aprobadas por el usuario).
- **Bloques HTML migrados y visibles en la aplicación (continuación)**: `LiveCountdown` (Sprint 3.9, ✅ — `<span>` de countdown con timer propio, visible en `RootLayout` dentro del bloque `role === 'coordinador'`, con props mock estáticas; validación real, visual y funcional aprobadas por el usuario).
- **Bloques HTML migrados y visibles en la aplicación (continuación)**: `InstallerDashboard` (Sprint 3.10, ✅ — barra del teléfono/`.mx-mesel`, navegación `.mx-phonetabs` y estado vacío de "Solicitudes" `mx-phone-empty`, visible en `RootLayout` dentro del bloque `role === 'instalador'`, reemplazando la integración ad-hoc del Sprint 3.2.1/3.2.2; validación real y visual aprobadas por el usuario).
- **Bloques HTML migrados y visibles en la aplicación (continuación)**: `InstallerProfile` (Sprint 3.11, ✅ — pantalla "Perfil" del teléfono del Instalador, `.mx-profscreen`, integrada dentro de la rama real `instTab === 'perfil'` de `InstallerDashboard` tras un ajuste de punto de integración aprobado por el usuario; validación real y visual aprobadas).
- **Bloques HTML migrados y visibles en la aplicación (continuación)**: `InstallerJobs` (Sprint 3.12, ✅ — pantalla "Mis trabajos" del teléfono del Instalador, `.mx-myjobs`, integrada directamente dentro de la rama real `instTab === 'trabajos'` de `InstallerDashboard`, sin ningún mount temporal en `RootLayout`; validación real, visual y funcional aprobadas).
- **Bloque migrado pero pendiente de cierre formal**: `mx-instside` / `InstallerSidebar` (Sprint 3.2 + sub-iteraciones 3.2.1/3.2.2). El código está construido, ajustado y visible en `RootLayout` cuando `role === 'instalador'`; la validación best-effort de este sandbox (`tsc`/`prettier`) está en verde, y el mismo código ya viajó, sin cambios adicionales, dentro de la rama `feature/sprint-3-3-mx-subtabs` cuyas 4 validaciones reales (`lint`/`typecheck`/`build`/`dev`) el usuario confirmó en verde. Aun así, el Sprint 3.2 **no se marca ✅ Completado** porque el usuario nunca dio la confirmación explícita puntual para ese Sprint — la regla del proyecto exige aprobación explícita por Sprint, no una inferencia a partir de la validación de un Sprint posterior.
- **Sprint 3.5 (`PublishModal`, formulario de publicar trabajo) — ✅ Completado.** Análisis, componente (`PublishModal`), constantes (`PROVINCIAS`/`ZONAS`/`BID_OPTIONS`/`SLOTS_COORD`), CSS e integración visual completos; validación real (`npm install/lint/typecheck/build/dev`) y validación visual confirmadas por el usuario. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.6 (`CoordinatorEmptyState`, estado vacío de `Coordinator`) — ✅ Completado.** Análisis, componente (`CoordinatorEmptyState`, reutiliza `EmptyState`/`Button` de Fase 3) e integración visual completos, cero CSS nuevo (ya portado); validación real (`npm run lint/typecheck/build/dev`) y validación visual confirmadas por el usuario, incluida la apertura correcta de `PublishModal` desde el botón "Publicar trabajo". El nombre "Job Cards" que traía esta tabla **no correspondía** al bloque real reconstruible ahora mismo — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.6.md`. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.7 (`Radar`, panel SVG de instaladores) — ✅ Completado.** Análisis, componente (`Radar`, único, sin sub-componentes), datos mock (`INSTALLERS`/`ELIGIBLE_ORDER`), utilidad (`hashAngle`), CSS (5 selectores nuevos) e integración visual temporal en `RootLayout` completos. El nombre "Radar" **sí correspondía** al bloque real, pero la arquitectura multi-componente sugerida en el brief original **no** — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.7.md`. Validación real (`npm install/lint/typecheck/build/dev`) y validación visual confirmadas por el usuario. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.8 (`CountRing`, anillo SVG de countdown) — ✅ Completado.** Análisis obligatorio (20 preguntas) e implementación completos: componente (`CountRing`, sin sub-componentes), utilidad reincorporada (`fmt`), cero CSS nuevo, integración visual temporal en `RootLayout` (dentro de `role === 'instalador'`, autorizada explícitamente por el usuario). El nombre "Countdown" **sí correspondía** a un bloque real (`CountRing`) — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.8.md`. Validación real (`npm install/lint/typecheck/build/dev`) y validación visual confirmadas por el usuario. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.9 (`LiveCountdown`, countdown de texto para trabajos en cola) — ✅ Completado.** Análisis previo obligatorio (localización exacta, análisis completo de HTML/JSX/lógica/props/dependencias) e implementación completos: componente (`LiveCountdown`, sin sub-componentes), `fmt` reutilizada sin duplicar, cero CSS nuevo, integración temporal en `RootLayout` (dentro de `role === 'coordinador'`, aplicada directamente per la nueva regla permanente de este Sprint, sin pausar a pedir autorización). El nombre "Timeline" que traía esta tabla **no correspondía** al bloque real reconstruible ahora — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.9.md`. Validación real (`npm install/lint/typecheck/build/dev`) y validación visual y funcional (timer corriendo en vivo) confirmadas por el usuario. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.10 (`InstallerDashboard`, subconjunto reconstruible de `Installer(props)`) — ✅ Completado.** Análisis previo obligatorio e implementación completos: componentes nuevos (`InstallerDashboard`, `InstallerSolicitudesEmptyState`, `MxPhoneTabs`), reutilización sin duplicar de `TwoColumnLayout`/`PhoneFrame` (Fase 3, primer consumidor real), `InstallerSidebar` (Sprint 3.2, ahora en su posición estructural real) y `MxSubtabButton` (Sprint 3.3), CSS nuevo (`.mx-phone-empty`), integración temporal en `RootLayout` (dentro de `role === 'instalador'`, reemplaza la integración ad-hoc del Sprint 3.2.1/3.2.2). El nombre "Installer Dashboard" que traía esta tabla **no correspondía** a ninguna función real — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.10.md`. Validación real (`npm install/lint/typecheck/build/dev`) y validación visual confirmadas por el usuario. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.11 (`InstallerProfile`, pantalla "Perfil" del teléfono del Instalador) — ✅ Completado.** Análisis previo obligatorio e implementación completos: componente nuevo (`InstallerProfile`, función pura sin estado/efectos propios), reutilización sin duplicar de `Badge` (`tone="green"`, Fase 3, para el helper `Pill` del HTML) e `INSTALLERS`/`InstallerMock` (ya existentes), CSS nuevo (bloque `.mx-prof*`, 12 reglas). Entrega inicial integrada temporalmente como hermano de `InstallerDashboard` en `RootLayout`; **ajuste posterior aprobado por el usuario** movió la integración a la rama real `instTab === 'perfil'` de `InstallerDashboard`, sin modificar `InstallerProfile`. El nombre "Installer Profile" que traía esta tabla **sí correspondía** exactamente a la función real — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.11.md`. Validación real (`npm install/lint/typecheck/build/dev`) y validación visual confirmadas por el usuario. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.12 (`InstallerJobs`, pantalla "Mis trabajos" del teléfono del Instalador) — ✅ Completado.** Análisis previo obligatorio e implementación completos: componente nuevo (`InstallerJobs`, función pura sin props/estado/efectos), constantes nuevas (`ESTADO`, mapeo completo de 6 estados portado íntegro aunque solo 3 se usen aquí; `MISJOBS`, mock de 4 trabajos), reutilización sin duplicar de `Badge` (Fase 3) para la Pill de estado, CSS nuevo (bloque `.mx-phonehdr`/`.mx-myjobs`/`.mx-myjob*`, 8 selectores). **Primera aplicación completa de la regla de integración vigente desde el Sprint 3.11**: se integró directamente dentro de la rama real `instTab === 'trabajos'` de `InstallerDashboard`, sin ningún mount temporal en `RootLayout.tsx`. También primera aplicación de la nueva regla de "preparación para Supabase" (datos como constantes reutilizables, componente puramente de presentación). El nombre "InstallerJobs" que traía esta tabla **sí correspondía** exactamente a la función real — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.12.md`. Validación real (`npm install/lint/typecheck/build/dev`), visual y funcional confirmadas por el usuario. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.13 (`AdminPanel`/`AdminInstaladores`, raíz del panel de Administrador) — ✅ Completado.** Análisis previo obligatorio e implementación completos: componentes nuevos (`AdminPanel`, orquestador de sub-tabs; `AdminInstaladores`, tabla de instaladores + formulario de invitación), reutilización sin duplicar de `MxSubtabs`/`MxSubtabButton` (Sprint 3.3, primer consumidor real fuera de Coordinator), `PageContainer`/`PageHead`/`Card`/`CardHeader`/`Badge`/`Button` (Fase 3) e `INSTALLERS`/`ZONAS` (Sprints 3.7/3.5, sin constantes nuevas), CSS nuevo (bloque `.mx-admingrid`/`.mx-admintable`/`.mx-adminrow*`/`.mx-admin-act`/`.mx-invite*`, 15 selectores + 1 media query). **Integración real y directa en `RootLayout.tsx`** (`role === 'admin'`, misma posición estructural que `App()` en el HTML fuente) — no un mount temporal, ya que el "contenedor" (`RootLayout.tsx` como equivalente de `App()`) existe desde el Sprint 3.1. El nombre "Admin Dashboard" que traía esta tabla **no correspondía** a ninguna función real — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.13.md`. `MasterCalendar` (contenido de "Calendario maestro") queda fuera de alcance, reservado para el Sprint 3.14. Validación real (`npm install/lint/typecheck/build/dev`), visual y funcional confirmadas por el usuario. Sin decisiones arquitectónicas permanentes nuevas que requirieran actualizar `ARCHITECTURE.md`. Sin pendientes técnicos para cerrar este Sprint.
- **Sprint 3.14 (`MasterCalendar`, calendario maestro del panel de Administrador) — ✅ Completado.** Análisis previo obligatorio e implementación completos: componente nuevo (`MasterCalendar`, sin sub-componentes), constantes nuevas (`SUSCOL`, colores por sucursal; `TRABAJOS`, mock de 13 trabajos con fecha real), reutilización sin duplicar de `PageContainer`/`PageHead`/`Card`/`Badge` (Fase 3), `SUCURSALES` (Sprint 3.4) y `ESTADO` (Sprint 3.12), CSS nuevo (bloque de 21 selectores `.mx-cal-*`/`.mx-joblist`/`.mx-jobrow*`/`.mx-suc-badge`). **Integración real y directa dentro de `AdminPanel`** (rama `tab === 'calendario'`, antes `null` desde el Sprint 3.13) — sin ningún mount temporal en `RootLayout.tsx`, que no requirió ningún cambio. El nombre "Calendar" que traía esta tabla **sí correspondía** exactamente a la función real — ver "Notas de alcance detectadas" abajo y `docs/sprints/sprint-3.14.md`. Se detectó y documentó un segundo punto de montaje real de `MasterCalendar` (`CoordinatorJobs({isMaster:true})`), fuera de alcance porque ese componente no existe todavía. Validación técnica (`npm run lint/typecheck/build/dev`), visual y funcional confirmadas por el usuario. Sin cambios de arquitectura — `ARCHITECTURE.md` no se modificó. Sin pendientes técnicos para cerrar este Sprint. Con este cierre, el módulo Admin queda completo en su alcance de reconstrucción visual (`AdminPanel`+`AdminInstaladores`+`MasterCalendar`).
- **Próximo Sprint a desarrollar**: 3.15 (Shared Dialogs, nombre genérico pendiente de análisis real del bloque HTML). **No se inicia sin el análisis previo obligatorio de su propio bloque HTML.**
- **Integraciones temporales activas en `src/layouts/RootLayout.tsx`** (a retirar cuando existan los layouts/flujos reales por rol): `SucursalSelect` + `MxSubtabs` + `CoordinatorEmptyState` + `Radar` + `LiveCountdown` (en ese orden) para `role === 'coordinador'` (Sprint 3.4 + 3.3 + 3.6 + 3.7 + 3.9), `InstallerDashboard` (que ya compone internamente `InstallerSidebar` y, desde los Sprints 3.11/3.12, `InstallerProfile`/`InstallerJobs` en sus ramas reales `instTab === 'perfil'`/`'trabajos'`) + `CountRing` para `role === 'instalador'` (Sprint 3.10, reemplaza la integración de Sprint 3.2.1/3.2.2 + Sprint 3.8), y `PublishModal` (Sprint 3.5) como hermano de ambos bloques de `role` — igual que en el HTML fuente, `PublishModal` no depende de ningún `role` específico; ahora se abre mediante el botón real `onOpenPublish` de `CoordinatorEmptyState` (`showPublishModal` ya no forzado, Sprint 3.6). Nota: ni `InstallerProfile` ni `InstallerJobs` tienen mount independiente propio en `RootLayout.tsx` — `InstallerProfile` sí lo tuvo en su entrega inicial (Sprint 3.11), pero un ajuste posterior lo movió dentro de `InstallerDashboard`; `InstallerJobs` (Sprint 3.12) se integró directamente ahí desde el principio, sin pasar por `RootLayout.tsx`. **`AdminPanel` (Sprint 3.13) es integración real y directa desde el principio** — `role === 'admin' && <AdminPanel />`, no es una integración temporal a retirar. **`MasterCalendar` (Sprint 3.14) también es integración real y directa desde el principio**, dentro de `AdminPanel` (`tab === 'calendario'`) — tampoco es una integración temporal a retirar.
- **Componentes creados sin consumidor todavía**: ninguno pendiente de `mx-subtabs`/`mx-instside`/`mx-suc-sel`/`PublishModal`/`CoordinatorEmptyState`/`Radar`/`CountRing`/`LiveCountdown`/`InstallerDashboard`/`InstallerProfile`/`InstallerJobs`/`AdminPanel`/`AdminInstaladores`/`MasterCalendar` (los catorce ya integrados, todos de forma real salvo las integraciones temporales de rol listadas arriba). La posible duplicación entre `ui/Tabs` (Fase 3, Radix, sin consumidores) y `MxSubtabs`/`MxSubtabButton`/`MxPhoneTabs` queda resuelta de facto para AdminPanel (el Sprint 3.13 reutilizó `MxSubtabs`, no `ui/Tabs`), aunque `ui/Tabs` en sí sigue sin consumidores — no se elimina ni se modifica.
- **Problemas conocidos, reportados sin corregir**: (1) el badge de sucursal de `HeaderStatus` (`sucursalActiva`, default `"Multiplaza"` desde Sprint 3.1) no recibe el `sucursalCoord` real que ahora controla `SucursalSelect` (Sprint 3.4) — quedan desincronizados; no se corrige porque requeriría modificar la invocación de `Header`, fuera del alcance mínimo permitido en el Sprint 3.4. (2) El botón "Publicar trabajo" de `PublishModal` (Sprint 3.5) invoca `onPublish` con una función vacía — `TRABAJOS` ya existe como mock estático de solo lectura desde el Sprint 3.14 (consumido por `MasterCalendar`), pero `onPublish` no escribe ahí ni en ningún otro lado: sigue sin existir un motor de trabajos real (`jobs`/`publishJob`) que conecte la publicación con una lista mutable; el botón real que lo abre ya existe desde Sprint 3.6 (`CoordinatorEmptyState`), pero la lógica de publicación en sí sigue pendiente. Ver `docs/sprints/sprint-3.4.md`, `docs/sprints/sprint-3.5.md` y `docs/sprints/sprint-3.6.md` respectivamente.

## Notas de alcance detectadas

- **3.2 (Sidebar → `mx-instside`)**: confirmado y resuelto en el Sprint 3.2. El único elemento visual llamado "sidebar" en el HTML es `.mx-instside` (panel lateral del Instalador, con las tarjetas "Tu perfil" y "Reglas de prioridad"). Detalle completo en `docs/sprints/sprint-3.2.md`.
- **3.3 (Top Navigation → `mx-subtabs`)**: confirmado y resuelto en el Sprint 3.3 (✅ Completado). El candidato era `.mx-subtabs-wrap`/`.mx-subtabs`, que en realidad tiene **dos** instancias reales en el HTML (Coordinator: "Despacho en vivo"/"Mis trabajos"; AdminPanel: "Calendario maestro"/"Instaladores"), ambas dependientes de pantallas que todavía no existen. Se reconstruyó el markup/CSS (`MxSubtabs`/`MxSubtabButton`) y se integró en `RootLayout` (visible para `role === 'coordinador'`) hasta que exista `layouts/CoordinatorLayout.tsx` real — ver `docs/sprints/sprint-3.3.md`.
- **3.4 (nombre genérico "Main Layout" → `mx-suc-sel`)**: el nombre original de esta tabla para el Sprint 3.4 era un placeholder especulativo ("Main Layout"), nunca confirmado por análisis real. Al hacer el análisis obligatorio se determinó que `.mx-grid`/`.mx-col` (candidato natural de "Main Layout") ya estaban migrados desde Fase 3 (`TwoColumnLayout`), y que el verdadero bloque pendiente, sin ningún avance previo (ni CSS ni componente), era `.mx-suc-sel` (selector de sucursal activa), hermano de `.mx-subtabs-wrap` dentro de la misma rama `role === "coord"` de `App()`. Se reconstruyó markup/CSS/componente (`SucursalSelect`) y se integró en `RootLayout` (visible para `role === 'coordinador'`, antes de `MxSubtabs`, mismo orden del HTML fuente) — ver `docs/sprints/sprint-3.4.md`.
- **3.5 ("Publish Modal" → `PublishModal`, confirmado)**: a diferencia del Sprint 3.4, el nombre genérico "Publish Modal" de esta tabla **sí correspondía** al bloque real — confirmado en el propio script (`function PublishModal(...)`, línea 2496). Se detectó y descartó un snapshot DOM obsoleto (`.mx-publishwrap`/`.mx-publish`/`.mx-pub-h`) que ya no aparece en ningún `React.createElement` del script vigente — ver "Discrepancias detectadas" en `docs/sprints/sprint-3.5.md`.
- **3.6 ("Job Cards" → `CoordinatorEmptyState`, corregido)**: el nombre genérico "Job Cards" de esta tabla **no correspondía** al bloque real reconstruible ahora mismo. Se leyó el cuerpo completo de `function Coordinator(props)` (líneas 2132-2423 del script): salvo su primera rama (`if (jobs.length === 0) return <div className="mx-qempty">...`, líneas 2146-2163), todo el resto de la función (`mx-jobcard`, `QueueBar`, Radar, `AssignedPanel`, respuestas) depende de `jobs.length > 0`. `jobs` arranca en `useState([])` en `App()` (línea 1898) y no existe ningún `SEED_JOBS`/mock en todo el HTML fuente — la única forma de poblarlo es `publishJob`, lógica de negocio fuera de alcance de este Sprint. Se migró en su lugar el único bloque reconstruible sin inventar datos/lógica: el estado vacío de `Coordinator` (`CoordinatorEmptyState`, reutilizando `EmptyState`/`Button` de Fase 3, sin CSS nuevo) — ver `docs/sprints/sprint-3.6.md`.
- **3.7 ("Radar" → `Radar`, confirmado con corrección de arquitectura)**: a diferencia del Sprint 3.6, el nombre genérico "Radar" de esta tabla **sí correspondía** al bloque real — confirmado en el propio script (`function Radar({ notified, instState, eligibleIds })`, líneas 1492-1745). Sin embargo, el brief de este Sprint sugería una arquitectura multi-componente (`RadarMap`/`RadarContainer`/`RadarMarker`/`RadarLegend`/`RadarOverlay`/`RadarControls`/`RadarInstallerCard`/`RadarStatusBadge`/`RadarInfoPanel`, bajo `src/components/radar/`) que **no corresponde a nada real del HTML** — es un único componente SVG autocontenido, sin sub-componentes propios. Se descartó esa arquitectura y se construyó únicamente `Radar` en `src/components/shared/`, siguiendo la convención ya establecida por el proyecto. También se confirmó y descartó explícitamente `CountRing` (líneas 1437-1491, vecino en el archivo fuente) de este Sprint: es un anillo de countdown sin relación visual con el radar, reservado para el Sprint 3.8 ("Countdown"). Ver `docs/sprints/sprint-3.7.md`.
- **3.8 ("Countdown" → `CountRing`, confirmado; hallazgo de un segundo componente real)**: el nombre genérico "Countdown" de esta tabla **sí correspondía** a un bloque real — confirmado en el propio script (`function CountRing({ remaining, total, size, color })`, líneas 1437-1491). El brief exigió un análisis previo de 20 preguntas antes de escribir código, y una segunda aprobación explícita del usuario antes de implementar. Se confirmó que `CountRing` no tiene ninguna clase CSS propia (todo inline) y no depende de `Coordinator`/`mx-jobcard`/`Radar`/Timeline. Se detectó y reportó, sin asumir, que el script tiene un **segundo** componente real de countdown, `LiveCountdown` (línea 2473, con timer propio, usado dentro de `Coordinator`/`mx-jobcard`) — distinto de `CountRing` y sin Sprint numerado todavía. Sus únicos usos reales de `CountRing` están dentro de `Installer(props)` (pantallas "alerta"/"oferta" del teléfono), que no existen todavía en el proyecto; se propuso y el usuario autorizó una integración temporal en `RootLayout.tsx` dentro de `role === 'instalador'`. Ver `docs/sprints/sprint-3.8.md`.
- **3.9 ("Timeline" → `LiveCountdown`, corregido)**: el nombre genérico "Timeline" de esta tabla **no correspondía** al bloque real reconstruible ahora mismo. El bloque real pendiente y ya identificado en el Sprint 3.8 era `LiveCountdown` (línea 2473, `function LiveCountdown({ publishedAt, bidMins })`) — un `<span>` de texto con countdown propio, único consumidor real `statusPill(jb)` dentro de `QueueBar`/`Coordinator`. Se detectaron y reportaron, sin asumir, dos discrepancias del brief de este Sprint contra el HTML real: (1) el brief asumía que `LiveCountdown` renderiza `CountRing` (Sprint 3.8) — falso, son componentes de countdown completamente independientes en el HTML fuente; (2) el brief asumía que dispara callbacks al expirar — falso, no existe ninguna prop de función en su firma real. Se implementó la versión verificada contra el HTML, no la asumida por el brief. Ver `docs/sprints/sprint-3.9.md`.
- **3.10 ("Installer Dashboard" → subconjunto de `Installer(props)`, corregido)**: el nombre genérico "Installer Dashboard" de esta tabla **no correspondía** a ninguna función real del HTML (no existe `function InstallerDashboard`). El equivalente real es `function Installer(props)` (líneas 3169-3452), componente raíz que `App()` monta cuando `role === "inst"`. Dentro de esa función, `InstallerJobs()` (`.mx-myjobs`) e `InstallerProfile()` (`.mx-profscreen`) son funciones reales, con su propio selector, ya reservadas como Sprints independientes (3.12 y 3.11) — no se invadió ese alcance. Se reconstruyó únicamente el resto: `.mx-instwrap` completo (dándole por primera vez un consumidor real a `TwoColumnLayout`/`PhoneFrame`, de Fase 3), la navegación `.mx-phonetabs` (nuevo `MxPhoneTabs`, reutilizando `MxSubtabButton` del Sprint 3.3) y el único estado de "Solicitudes" reconstruible sin motor de trabajos (`mx-phone-empty`, mismo criterio que `CoordinatorEmptyState` en el Sprint 3.6). La integración ad-hoc de `InstallerSidebar` del Sprint 3.2.1/3.2.2 (con un Phone Placeholder vacío) se retiró, reemplazada por esta composición real. Ver `docs/sprints/sprint-3.10.md`.
- **3.11 ("Installer Profile" → `InstallerProfile`, confirmado)**: a diferencia de la mayoría de los Sprints anteriores de esta fase, el nombre genérico "Installer Profile" de esta tabla **sí correspondía** exactamente al bloque real — confirmado en el propio script (`function InstallerProfile({ meInfo })`, líneas ~3491-3524, `.mx-profscreen`). Se reconstruyó verbatim: avatar/nombre/zona, Pill "Instalador verificado" (reutiliza `Badge tone="green"`), 4 estadísticas y "Reglas de prioridad" de 4 ítems (distinta de la versión de 5 ítems de `mx-instside`, ya migrada en el Sprint 3.2 — no se unificaron). Entrega inicial integrada temporalmente como hermano independiente de `InstallerDashboard` en `RootLayout.tsx` (el brief de este Sprint prohibía modificar `InstallerDashboard`); un ajuste posterior, aprobado explícitamente por el usuario tras la validación visual, movió la integración a la rama real `instTab === 'perfil'` de `InstallerDashboard` — sin modificar `InstallerProfile` (componente, lógica, estilos y estructura intactos). Ver `docs/sprints/sprint-3.11.md`.
- **3.12 ("InstallerJobs" → `InstallerJobs`, confirmado)**: el nombre "InstallerJobs" de esta tabla **sí correspondía** exactamente al bloque real — confirmado en el propio script (`function InstallerJobs()`, líneas 3453-3484, `.mx-myjobs`, sin props). Se reconstruyó verbatim: agrupación "Próximos"/"Historial" (`.mx-phonehdr`), tarjetas de trabajo (`.mx-myjob`) con tipo, Pill de estado (reutiliza `Badge`, resuelto vía la nueva constante `ESTADO`), zona, fecha/hora y precio. A partir de este Sprint rigen dos reglas permanentes nuevas: (1) integración directa dentro del contenedor real cuando este ya existe (`InstallerDashboard`, sin ningún mount temporal en `RootLayout.tsx` — primera vez que se aplica sin pasos intermedios); (2) preparación para Supabase (datos como constantes reutilizables `MISJOBS`/`ESTADO`, no generados dentro del componente; componente puramente de presentación). Ver `docs/sprints/sprint-3.12.md`.
- **3.13 ("Admin Dashboard" → `AdminPanel`/`AdminInstaladores`, corregido)**: el nombre genérico "Admin Dashboard" de esta tabla **no correspondía** a ninguna función real — verificado con `grep -n "function Admin"` sobre el HTML completo, sin resultado para "AdminDashboard". El equivalente real es `function AdminPanel()` (líneas 3031-3048 del script), componente raíz que `App()` monta cuando `role === "admin"`, análogo a `Coordinator(props)`/`Installer(props)` para los otros roles. `AdminPanel()` compone sub-tabs (`.mx-subtabs-wrap`/`.mx-subtabs`, reutilizando `MxSubtabs`/`MxSubtabButton` del Sprint 3.3 — su propio JSDoc ya anticipaba este momento) con dos pestañas reales: "Calendario maestro" → `MasterCalendar` (función real, sin construir, reservada para el Sprint 3.14 "Calendar" — no se invadió su alcance) e "Instaladores" → `AdminInstaladores()` (líneas 3049-3160, `.mx-page`/`.mx-pagehead`/`.mx-admingrid`), reconstruida en este mismo Sprint por depender únicamente de `INSTALLERS`/`ZONAS`, ya migrados. Se detectó y documentó que el HTML fuente estiliza el formulario de invitación de `AdminInstaladores` vía el selector descendiente `.mx-invite input,.mx-invite select`, no vía las clases genéricas `.mx-input`/`.mx-select-native` — por eso se reconstruyeron como elementos nativos, a diferencia de `PublishModal` (Sprint 3.5). Integración real y directa en `RootLayout.tsx` (`role === 'admin'`, misma posición estructural que `App()`), no temporal. Ver `docs/sprints/sprint-3.13.md`. Validación real (`npm install/lint/typecheck/build/dev`), visual y funcional confirmadas por el usuario — **Sprint 3.13 ✅ Completado.**
- **3.14 ("Calendar" → `MasterCalendar`, confirmado)**: el nombre "Calendar" de esta tabla **sí correspondía** exactamente al bloque real — confirmado en el propio script (`function MasterCalendar()`, líneas 2825-3028), ya identificado y reservado desde el análisis del Sprint 3.13. Se reconstruyó verbatim: filtro de sucursal, grilla de mes con navegación (‹/›), puntos de color por trabajo, día de hoy resaltado, lista de trabajos del día seleccionado (reutiliza `Badge` para la Pill de estado, resuelto vía `ESTADO`) y leyenda de colores por sucursal (nueva constante `SUSCOL`, 9 entradas, y nuevo mock `TRABAJOS`, 13 trabajos con fecha real). Se detectó y documentó (no una diferencia de nombre) que el HTML fuente también monta `MasterCalendar` desde `CoordinatorJobs({isMaster:true})` (línea 2661) — ese segundo punto de integración queda fuera de alcance porque `CoordinatorJobs()` no está construido todavía, sin Sprint asignado. Integración real y directa dentro de la rama `tab === 'calendario'` de `AdminPanel` (antes `null`, Sprint 3.13) — sin ningún mount temporal en `RootLayout.tsx`. Ver `docs/sprints/sprint-3.14.md`. Validación técnica (`npm run lint/typecheck/build/dev`), visual y funcional confirmadas por el usuario — **Sprint 3.14 ✅ Completado.**
- El bloque `mx-instside` migrado en 3.2 es hermano de `.mx-phone` dentro de `.mx-instwrap` — desde el Sprint 3.10, ese wrapper y el teléfono (`mx-phone`/`mx-phone-bar`/`mx-phonetabs`) ya tienen su propio componente real (`InstallerDashboard`), aunque todavía no vive dentro de un `layouts/InstallerLayout.tsx`/ruta dedicada (`ARCHITECTURE.md` §3) — sigue integrado temporalmente en `RootLayout`. El Sprint 3.6, ya cerrado, confirmó que no cubría ese layout (migró el estado vacío de `Coordinator`, no `mx-instwrap`/`mx-phone`). El Sprint 3.7 confirmó lo mismo (migró `Radar`). El Sprint 3.8 migró `CountRing` (integrado temporalmente dentro de `role === 'instalador'`, como hermano de `InstallerDashboard`, no como parte de su grid — su lugar real, dentro de `mx-alert-h`/`mx-offer-h`, sigue sin construirse).
- El HTML fuente tiene dos versiones de "Reglas de prioridad" (5 ítems en `mx-instside`, ya migrados en 3.2; 4 ítems dentro de `InstallerProfile()`/`mx-profblock`, ya migrados en 3.11). Ambas versiones se mantienen sin unificar, tal como le corresponde a cada bloque de origen. Ver `docs/sprints/sprint-3.2.md` → "Dependencias detectadas" y `docs/sprints/sprint-3.11.md`.
- **Legado de Fase 3**: antes de adoptar esta metodología de Sprints, el proyecto se desarrolló por fases grandes (ver `PROJECT_STATUS.md`/`CHANGELOG.md`, Fases 1–3). La Fase 3 ("Layout general y componentes compartidos") ya había reconstruido gran parte de Header/Sidebar/PhoneFrame/Footer y una librería `components/ui/` completa, pero incluía una página de vitrina (`LayoutShowcasePage`) — eliminada en el Sprint 3.1 por ser incompatible con la nueva regla "no crear una vitrina de componentes". Los componentes reutilizables de Fase 3 que sí corresponden a elementos reales del HTML se mantienen y se van ajustando Sprint a Sprint para llevarlos a fidelidad exacta (ver `MIGRATION_STATUS.md`).

## Detalle por Sprint (histórico completo)

Para el análisis previo, implementación, correcciones y validaciones línea por línea de cada Sprint (incluidas sus sub-iteraciones 3.2.1, 3.2.2, y el fix de integración visual de 3.3), ver:

- `docs/sprints/sprint-3.1.md`
- `docs/sprints/sprint-3.2.md` (incluye 3.2.1 y 3.2.2)
- `docs/sprints/sprint-3.3.md` (incluye el fix de integración visual)
- `docs/sprints/sprint-3.4.md`
- `docs/sprints/sprint-3.5.md`
- `docs/sprints/sprint-3.6.md`
- `docs/sprints/sprint-3.7.md`
- `docs/sprints/sprint-3.8.md`
- `docs/sprints/sprint-3.9.md`
- `docs/sprints/sprint-3.10.md`
- `docs/sprints/sprint-3.11.md`
- `docs/sprints/sprint-3.12.md`
- `docs/sprints/sprint-3.13.md`
- `docs/sprints/sprint-3.14.md`

Desde Fase 4, el detalle por Sprint se documenta en `docs/architecture/frontend/SPRINT_<fase>_<n>_<nombre>_REPORT.md` en vez de `docs/sprints/sprint-X.Y.md` (convención vigente desde Sprint 4.1.1B). Para Sprint 5.1 ("Dashboard y Vista Operativa del Coordinador"), ver `docs/architecture/frontend/SPRINT_5_1_COORDINATOR_REPORT.md`.
