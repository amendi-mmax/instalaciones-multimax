/** Footer — portado verbatim de `.mx-foot`. */
export function Footer() {
  return (
    <footer className="mx-foot">
      Las dos vistas — Coordinador e Instalador — comparten el mismo trabajo en vivo.
    </footer>
  );
}
